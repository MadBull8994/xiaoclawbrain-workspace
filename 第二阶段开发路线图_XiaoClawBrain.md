# XiaoClawBrain 第二阶段开发路线图

创建时间：2026-06-04  
用途：作为 P0-P9 第一阶段稳定验收完成后的统一后续路线图。  
最近对齐：2026-06-04

---

## 当前对齐结论

项目主方向不变：

```text
ESP32-S3 瘦客户端 + xiaozhi-esp32-server 胖后端
```

第一阶段已经完成的结论保持不变：

- P0-P9 核心闭环已完成
- P6 旧架构清理已收口
- P9 本地语音唤醒已于 2026-06-02 完成 10/10 实机稳定验收
- 当前稳定基底为 `XCB-STABLE-P9-complete+P6clean+20260602.123952`

2026-06-04 复核结论：

- 未发现需要重新打开第一阶段验收结论的新增功能性阻塞
- 当前“没收干净的尾巴”主要是文档层面的阶段切换口径残留，不是第一阶段主链路缺失
- 因此后续工作不再以零散 `P10` 或“第一版暂不做”方式推进，而是统一进入第二阶段路线

第二阶段当前推荐入口：

```text
第二阶段 P1：配置与持久化基线
```

---

## 第二阶段边界

第二阶段做的是第一阶段完成后的增强、沉淀和扩展，不把这些工作倒算成第一阶段缺陷：

- 第一阶段明确暂缓但仍有价值的能力
- 已记录为候选升级的能力
- 新增的个性化、默认配置、持久化和后台对齐需求

第二阶段仍沿用以下约束：

- 优先做最小可用改动，不做无关重构
- 跨端协议变更先更新文档，再分别实现后端和 ESP32
- 正常更新不得破坏 `data/`、`mysql/data/`、设备 `nvs`、`fatfs` 等持久化状态
- 不把归档目录或旧本地 AI 架构重新并回主线

---

## 总体执行顺序

### 第二阶段 P1：配置与持久化基线

目标：先把“哪些东西更新后必须保留”讲清楚，避免后面做了个性化能力却在升级时丢失。

任务：

- 明确后端正常更新时必须保留的目录和数据边界
- 明确设备正常刷机时允许与禁止擦除的范围
- 确定以下默认项的权威存储位置：
  - 默认全局角色 Skill
  - 默认百度克隆音色
  - 默认 Agent persona
- 记录“仓库默认值”和“用户长期默认值”的职责边界

验收：

- 文档明确说明正常更新保留什么、危险操作会清掉什么
- 默认 persona / voice / skill 不再只依赖代码仓库里的默认文件
- 第二阶段后续能力都有稳定配置落点可用

### 第二阶段 P2：角色、声音与默认 Skill 自定义

目标：把当前用户关心的默认人格、默认声音、默认全局 Skill 做成正式、可维护、可持久化的能力。

任务：

- 定义默认全局角色 Skill 的安装、启用和替换流程
- 定义默认百度克隆音色的选择和回退流程
- 定义默认 Agent persona 的配置入口与运行时读取方式
- 确保上述默认项能跨正常更新保留

验收：

- 默认角色、声音、Skill 都有明确的生效路径
- 更新后不会因为镜像重建或普通刷机把用户默认项重置掉
- 文档能指导后续实现和运维

### 第二阶段 P3：会话模式与记忆策略升级

目标：把旧 `P10` 候选并入正式路线，形成可实现、可验证的会话模式升级方案。

任务：

- 增加默认单轮与可选连续对话的配置设计
- 定义 follow-up 等待窗口、结束词、超时和异常收口
- 定义连续模式下的短期/长期记忆保存与查询策略
- 明确设备端是否需要新增 `followup_wait` 或等价状态展示

验收：

- 单轮模式保持当前稳定行为
- 连续模式边界清晰，不与 wake word / TTS 尾音 / VAD 边界打架
- 记忆策略可解释、可验证

### 第二阶段 P4：交互体验增强

目标：集中处理第一阶段刻意暂缓的交互增强，而不是零散插入主链路。

任务：

- 用户打断 AI
- TTS 播放中重新拾音
- 更自然的一体化说法，例如“你好小智现在几点”的体验优化
- 其他不改变主架构、但能明显改善体感的对话细节

验收：

- 每项增强都有独立状态机边界与失败回退策略
- 不破坏第一阶段已有稳定链路

### 第二阶段 P5：系统与平台增强

目标：收纳明显更重的基础设施或产品化能力。

任务：

- OTA
- 多设备后台 / 管理能力
- 后端 Cron / 提醒 / 任务执行
- 复杂长期记忆
- 更广泛的外部集成能力

验收：

- 每项能力都先有架构和边界，再进入实现
- 不和第一阶段、第二阶段前半段的配置归属冲突

### 第二阶段 P6：后台 UI 与配置页对齐评估

目标：复核当前后台页面、配置页和真实系统能力是否一致。

任务：

- 检查后台 UI 是否暴露了当前并不存在或尚未稳定的能力
- 检查哪些配置应该在 UI 中可见，哪些应该继续留在配置文件或数据库
- 记录需要保留、调整或延后的页面项

验收：

- UI 文案与真实能力一致
- 配置归属不混乱，不出现“页面可改但实际不生效”或“真实默认项没有入口”的情况

---

## 推荐配置归属

第二阶段优先采用以下配置归属原则：

### 1. 默认全局角色 Skill

推荐位置：

- `xiaozhi-esp32-server/main/xiaozhi-server/data/skills/enabled/<skill-name>/SKILL.md`

推荐激活点：

- `skills.global_defaults.selected`

原因：

- 位于 `data/` 下，正常后端更新时更容易保留
- 避免把用户长期使用的角色 Skill 绑死在仓库示例文件里

### 2. 默认百度克隆音色

推荐归属：

- 以数据库或管理配置中的运行时默认值为主
- `data/.config.yaml` 只作为本地 fallback / bootstrap

原因：

- 用户选择的默认音色不应只依赖镜像内默认文件
- 后端镜像重建时需要能保留当前默认值

### 3. 默认 Agent Persona

推荐归属：

- 运行时主默认值优先放在 agent/template 配置中
- 文件型 fallback 可放在 `data/prompts/default_persona.txt`

原因：

- 仓库内 prompt 文件可以继续作为模板
- 但用户长期 persona 不应只放在代码跟踪的默认 prompt 里

---

## 当前开工建议

第二阶段建议按以下顺序开工：

1. 先做 **第二阶段 P1：配置与持久化基线**
2. 再做 **第二阶段 P2：角色、声音与默认 Skill 自定义**
3. 然后再决定是否启动 **第二阶段 P3：会话模式与记忆策略升级**

这样可以先把“默认值放哪里、更新后怎么保住”收稳，再往交互增强和更重的平台能力上走。
