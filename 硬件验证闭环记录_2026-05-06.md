# ESP32-S3 语音陪伴设备硬件验证闭环记录

日期：2026-05-06  
设备：ESP32-S3-N16R8 + 小智AI扩展板44P V1.1  
串口：`/dev/cu.usbmodem5B5E1028821`

## 1. 总结结论

硬件验证闭环已完成。PSRAM、TFT、INMP441 麦克风、MAX98357A 功放写入链路、四个按键链路均已验证通过。

最终硬件定稿中，8 针 TFT 继续使用 `GPIO40` 作为 `DC`，音量+ 因与 `TFT_DC` 存在跳帽/复用冲突，已通过飞线改接到 `GPIO38`。固件中音量+ 必须按 `GPIO38` 处理。

## 2. 最终引脚定稿

| 模块 | 信号 | GPIO / 连接 | 结论 |
|---|---:|---:|---|
| 主控 | Flash / PSRAM | 16MB Flash + 8MB PSRAM | 通过 |
| INMP441 | WS | GPIO4 | 通过 |
| INMP441 | SCK | GPIO5 | 通过 |
| INMP441 | SD | GPIO6 | 通过 |
| INMP441 | L/R | GND | 左声道输出，通过 |
| MAX98357A | DIN | GPIO7 | I2S 写入通过 |
| MAX98357A | BCLK | GPIO15 | I2S 写入通过 |
| MAX98357A | LRC | GPIO16 | I2S 写入通过 |
| TFT ST7735 | SCK | GPIO21 | 通过 |
| TFT ST7735 | SDA/MOSI | GPIO47 | 通过 |
| TFT ST7735 | RES | GPIO45 | 通过 |
| TFT ST7735 | DC | GPIO40 | 通过，保留给 TFT |
| TFT ST7735 | CS | GPIO41 | 通过 |
| TFT ST7735 | BL | GPIO42 | 通过 |
| 按键 | VOL- | GPIO39 | 通过 |
| 按键 | VOL+ | GPIO38 | 通过，飞线改接 |
| 按键 | MUTE/BOOT | GPIO0 | 通过 |
| 按键 | RESET | RST | 通过，硬件复位 |
| RGB LED | WS2812 | GPIO48 | 可用于运行状态指示 |

## 3. 验证过程

### 3.1 基础识别

- 芯片识别：ESP32-S3 QFN56 rev v0.2。
- Flash：16MB。
- PSRAM：8MB，内存测试通过。
- MAC：`ac:a7:04:ee:12:78`。

### 3.2 麦克风链路

最初 INMP441 无有效音频，后续定位到麦克风模块 `SD` 与 `VDD` 短接。拆开短接并更换/重新处理模块后，重新测试通过。

最终通过结果：

- `WS(GPIO4)` 有时钟跳变。
- `SCK(GPIO5)` 有时钟跳变。
- `SD(GPIO6)` 有数据跳变。
- 原始 I2S 非全 0 / 非全 1。
- 左声道有效峰值约 `57000`，右声道为 `0`，符合 `L/R=GND` 时左声道输出预期。

### 3.3 TFT 屏幕

TFT ST7735 SPI 写入通过。屏幕能够显示测试提示，后续按键验证程序也成功在屏幕上显示 `TAP VOL+`、`VOL+ OK` 等状态。

关键结论：

- 8 针 TFT 的 `DC` 使用 `GPIO40`。
- 接 8 针 TFT 时需要按卖家说明处理跳帽，否则原版固件存在管脚冲突。

### 3.4 按键链路

原理图显示：

- `VOL+` 原始设计接 `GPIO40` 到 GND。
- `VOL-` 接 `GPIO39` 到 GND。
- `MUTE/BOOT` 接 `GPIO0` 到 GND。
- `RESET` 接 `RST` 硬件复位。

四键测试结果：

- `VOL- / GPIO39`：通过。
- `MUTE / GPIO0`：通过。
- `RESET / RST`：通过，重启后由 NVS pending 标记确认。
- `VOL+ / GPIO40`：在拔掉 TFT 跳帽后无法检测到电平变化。

进一步专项诊断显示：`GPIO40` 在 15 秒采样窗口内始终为高电平，`transitions=0`，说明问题不是软件窗口错过，而是跳帽/硬件复用导致 `VOL+` 支路未接入 `GPIO40`。

### 3.5 VOL+ 飞线改造

根据原理图和卖家反馈，`GPIO40` 同时承担 `TFT_DC` 和 `VOL+`，两者不能在当前接法下同时可靠使用。最终采用外部飞线方案：

- 跳帽保持拔掉，保证 8 针 TFT 正常。
- `TFT_DC` 继续使用 `GPIO40`。
- `VOL+` 按键非 GND 脚飞线到 `GPIO38`。
- 固件中将 `BTN_VOL_UP_PIN` 改为 `38`。

复测结果：

```text
GPIO38 空闲电平=1
GPIO38 变化: 1 -> 0
GPIO38 变化: 0 -> 1
汇总: transitions=36
[PASS] GPIO38 能检测到 VOL+ 电平变化。
```

结论：飞线改造成功。

## 4. 对方案的影响

需要更新方案中的以下内容：

1. 音量+ 不能继续写作 `GPIO40`，正式固件应改为 `GPIO38`。
2. `GPIO40` 必须保留给 TFT `DC`。
3. 音量键不是纯硬件增益控制，应由固件读取 GPIO 后调整播放音量，并将音量值保存到 NVS。
4. 硬件风险清单需要增加“GPIO40 与 TFT_DC / VOL+ 复用冲突”，并记录已通过飞线规避。
5. 后续正式固件的按键定义应为：

```c
#define BTN_VOL_UP_PIN   38
#define BTN_VOL_DN_PIN   39
#define BTN_MUTE_PIN      0
#define TFT_DC_PIN       40
```

## 5. 当前状态

硬件 bring-up 阶段完成，正式固件 MVP 的第一轮联网底座已经完成。2026-05-07 项目状态：`API TEST` 固件逻辑已实现、编译通过、刷写完成，并完成 DeepSeek `/models` 与最小 `/chat/completions` 串口实测；`ASR TEST` 已完成百度实时语音识别 WSS 实测并返回有效 `FIN_TEXT`。

已完成并上板验证：

1. 固定最终引脚表，正式固件使用 `VOL+=GPIO38`、`VOL-=GPIO39`、`BOOT=GPIO0`、`TFT_DC=GPIO40`。
2. `BOOT(GPIO0)` 不再作为静音键使用，改为功能键：短按进入设置菜单，菜单内再次短按选择。
3. TFT 启动自检页显示 PSRAM、MIC、SPK、KEYS、Wi-Fi 状态。
4. 音量 `VOL+ / VOL-` 已改为软件音量控制，音量值写入 NVS，重启后恢复。
5. 功放已按当前软件音量播放提示音，确认音量键会影响播放响度。
6. 麦克风采样任务常驻运行，TFT 显示当前输入峰值。
7. Wi-Fi AP 配网已完成：设备开启 `OpenClaw-Setup`，手机访问 `http://192.168.4.1` 写入 SSID/密码。
8. Wi-Fi 信息已写入 NVS，重启后可自动连接已保存网络。
9. 外网最小探测已完成：菜单 `NET TEST` 请求 `http://example.com/`，实测返回 `HTTP 200`。
10. SNTP 时间同步已完成：连接 Wi-Fi 后自动向 `pool.ntp.org` 同步时间，实测返回有效 epoch。

当前实测网络状态：

```text
SSID: MadBull
IP: 192.168.31.128
NET TEST: HTTP 200
SNTP: OK, epoch=1778076020, local=2026-05-06 22:00:20 CST
```

2026-05-12 追加验证：

```text
VOICE LLM：新增 ASR->LLM 半闭环菜单入口
前置检查：Wi-Fi CONNECTED + SNTP OK
录音规格：2 秒 / 16kHz / mono / PCM16 / 64000 bytes
ASR：百度 realtime WSS status=101，成功链路返回 err_no=0 / FIN_TEXT
ASR 文本：Hello。
LLM：DeepSeek /chat/completions HTTP 200
LLM 回复摘要：您好，语音测试中。
错误路径：曾复现 ASR 在 FIN_TEXT 前断开，分类 TRANSPORT，LLM 未触发
根因定位：Wi-Fi power save type=1 时，实时 ASR 音频上传中途出现约 1.5 秒发送卡顿
修复结果：设置 WIFI_PS_NONE 后，音频 chunk 约 160-170ms 连续发送，ASR 返回 FIN_TEXT，LLM 返回 HTTP 200
安全日志：API Key / ASR Key 仍只输出 SET/EMPTY，不输出明文
TTS TEST：已新增单项菜单入口和固件实现，开发阶段 provider=百度普通短文本 TTS
TTS 路径：OAuth token -> POST https://tsn.baidu.com/text2audio -> aue=4 PCM16 16k mono -> I2S 功放播放
TTS 状态：上板验证通过，OAuth HTTP 200，text2audio HTTP 200，Content-Type=audio/basic;codec=pcm;rate=16000;channel=1，音频 114240 bytes，I2S 播放 OK
TTS 安全日志：TTS API Key / Secret Key / token 只输出 SET/EMPTY 或 token=SET，不输出明文
冻结前最终回归：已用完整开发阶段 ASR + TTS + DeepSeek 凭据重刷正常菜单触发版；VOICE LLM 返回 ASR FIN_TEXT、DeepSeek HTTP 200、回复摘要非空；TTS TEST 返回 token HTTP 200、text2audio HTTP 200、PCM16 16k mono 114240 bytes 并播放 OK
音量策略：平时保持 volume=0；仅 TTS TEST 前手动调到 10，测试后已恢复 volume=0
安全日志：DeepSeek Key、百度 AppID/AppKey/Secret/token 均不输出明文，只显示 SET/EMPTY 或 token=SET
冻结结论：hardware_bringup 冻结为硬件/API/音频/网络诊断工具，不再扩展完整 Agent、skill、记忆、提醒或正式语音闭环系统；下一阶段进入 XiaoClaw 正式集成
```

2026-05-14 XiaoClaw 正式集成第一阶段进展：

```text
源码：已通过 GitHub 国内镜像源拉取 XiaoClaw，工作副本 xiaoclaw-ghproxycom，对应上游 https://github.com/beancookie/xiaoclaw
Board：新增 openclaw-v2.7 profile
引脚：TFT_DC=GPIO40，VOL+=GPIO38，VOL-=GPIO39，BOOT=GPIO0，MIC=GPIO4/5/6，SPK=GPIO7/15/16
显示：ST7735 TFT 参数已迁入 board profile
按键：BOOT / VOL+ / VOL- 已接入 XiaoClaw board 层
音频：I2S 麦克风与 I2S 功放使用 Simplex 通道启动
音量：软件音量已接入，支持 NVS 保存；启动音量允许为 0
分区：新增 openclaw_v2_7_16m.csv，适配 16MB flash 与 OTA
Wi-Fi 省电：board 层已加入会话结束后延迟恢复 LOW_POWER 的基础策略
TTS：新增百度普通 TTS provider 基础实现，使用 open/write/fetch_headers/read 分块读取 PCM，避免 HTTP event callback 写 PSRAM
TTS 接入：Mimiclaw Agent 回复路径已接入本地百度 TTS；未配置 TTS Key/Secret 时保留原后端 TTS 行为
构建：idf.py build 通过，xiaozhi.bin=0x319fb0，app 分区剩余 0xe6050，约 22%
刷写：/dev/cu.usbmodem5B5E1028821 通过
启动：16MB flash / 8MB PSRAM OK，OpenClaw V2.7 board initialized，NoAudioCodec Simplex channels created
配网：AP Xiaozhi-1279 正常启动，提示音播放后无崩溃
Release：releases/v2.2.4_openclaw-v2.7.zip 已生成
安全日志：DeepSeek Key、百度 AppID/AppKey/Secret/token 仍不得写入源码、文档或日志明文；XiaoClaw 当前 TTS Key/Secret 默认空值
未完成：XiaoClaw 侧百度 realtime ASR adapter 尚未接入，正式 ASR->LLM->TTS 闭环尚未完成
```

下一步建议：

1. DeepSeek 官方 OpenAI-compatible API 已完成 `API TEST`：`GET https://api.deepseek.com/models` 返回 HTTP 200。
2. 默认 `https://api.openai.com/v1` 在本轮网络下连接阶段 10 秒超时，固件已正确分类为 `TIMEOUT`。
3. DeepSeek 最小 `POST https://api.deepseek.com/chat/completions` 文本探测已完成，模型名 `deepseek-v4-flash` 和请求体可用，返回 HTTP 200。
4. API Base URL、API Key、测试模型名已迁移到 NVS 和配网页面；Key 留空提交时保留当前已保存 Key。
5. `REC TEST` 已完成 2 秒 16k mono PCM16 录音缓冲验证。
6. `ASR TEST` 已完成百度实时语音识别 WSS 探测入口：证书校验通过，WebSocket 握手返回 `101`。
7. `ASR TEST` 已支持 `dev_pid=1537 / 15372` 现场切换复测；最终使用 `dev_pid=15372` 返回 `err_no=0` / `FIN_TEXT`。
8. 此前 `err_no=-3004` 根因已定位为固件编入旧 AppID，导致 AppID/API Key 组合错配；修正当前百度应用 AppID 后已通过。
9. `hardware_bringup` 已新增 `VOICE LLM` 半闭环入口，并完成 ASR FIN_TEXT -> DeepSeek HTTP 200 的上板成功链路。
10. `VOICE LLM` 曾复现 ASR `TRANSPORT` 错误路径，已定位为 Wi-Fi 省电导致实时音频上传卡顿；关闭 Wi-Fi power save 后复测通过。
11. `hardware_bringup` 已新增并上板验证 `TTS TEST` 单项入口，开发阶段选用百度普通短文本 TTS，请求 PCM16 16k mono 并通过现有 I2S 功放播放路径输出。
12. 已完成冻结前最终回归：完整开发阶段 ASR + TTS + DeepSeek 凭据重刷，`VOICE LLM` 和 `TTS TEST` 均通过。
13. `hardware_bringup` 应作为硬件/API 诊断工具保留，不再扩展成最终产品系统。
14. 当前进入 XiaoClaw 正式集成，在 XiaoClaw 上承载 ASR、LLM、TTS、skill、记忆和提醒等产品能力。
15. XiaoClaw `openclaw-v2.7` board profile、构建、刷写、启动验证和本地百度 TTS provider 基础接入已完成。
16. 下一步继续 XiaoClaw 百度 realtime ASR adapter，并把 ASR 文本送入现有 Agent/LLM 流程。

## 6. API TEST 固件实现状态

2026-05-07 已完成代码实现和上板验证：

- 菜单新增 `API TEST`，保留 `NETWORK`、`NET TEST`、`TIME SYNC`、`VOL+ / VOL-` 等既有入口。
- API 配置结构包含 `API Base URL`、`API Key`、`测试模型名`，当前支持编译期默认值、构建环境变量覆盖和 NVS 持久化。
- Wi-Fi 配网页面已增加 `API Setup`，可以保存 Base URL、API Key 和模型名；API Key 留空时保留当前 Key。
- 菜单新增 `REC TEST`，用于录制 2 秒 PCM16 音频缓冲并输出样本数、字节数、Peak、RMS。
- HTTPS 使用 ESP-IDF 内置 CA 证书包，当前请求 OpenAI-compatible `/chat/completions` 最小文本接口。
- 请求前检查 Wi-Fi 必须为 `CONNECTED`，系统时间必须通过 SNTP 有效性检查。
- SNTP 改为多服务器：`pool.ntp.org`、`time.google.com`、`ntp.aliyun.com`。
- TFT 显示 `API TEST` 状态、HTTP 状态码和错误类别。
- 主机侧新增纯逻辑测试，覆盖 API 错误分类和 chat 响应体基础判断。
- 主机侧新增 API 配置纯逻辑测试，覆盖 Base URL、模型名、Key 校验，以及表单 Key 留空保留旧 Key。

串口验证结果：

```text
串口：/dev/cu.usbmodem5B5E1028821
Wi-Fi：connected with MadBull
IP：192.168.31.128
SNTP：Time sync OK: 1778113345
API TEST：base=https://api.openai.com/v1 endpoint=/models model=gpt-4o-mini key=EMPTY
API TEST 结果：class=TIMEOUT ret=ESP_ERR_HTTP_CONNECT errno=0 elapsed=10023ms HTTP=0
DeepSeek API TEST：base=https://api.deepseek.com endpoint=/models model=deepseek-v4-flash key=SET
DeepSeek API TEST 结果：Certificate validated, HTTP 200
DeepSeek CHAT TEST：base=https://api.deepseek.com endpoint=/chat/completions model=deepseek-v4-flash key=SET
DeepSeek CHAT TEST 结果：Certificate validated, HTTP 200, object=chat.completion
API CONFIG：启动日志显示 base=https://api.deepseek.com model=deepseek-v4-flash key=SET
API CONFIG 边界：SNTP 无效时 API TEST 拒绝请求并显示 NO TIME；时间有效后 API TEST 返回 HTTP 200
REC TEST：samples=32000 bytes=64000 peak=2975 rms=322 duration_ms=2000
ASR TEST：Baidu WSS status=101, err_no=0, type=FIN_TEXT, result=您好。
ASR TEST PID：15372 已通过，1537/15372 切换入口保留
ASR TEST 根因记录：此前 -3004 是旧 AppID 与当前 API Key 错配
```

结论：

- `API TEST` 菜单、前置检查、HTTPS 请求发起、超时分类、TFT/串口错误输出链路已验证。
- 默认 Base URL 到 `api.openai.com` 在当前网络下连接超时，且固件默认 API Key 为空。
- DeepSeek 官方 API 已拿到 `/models` 和 `/chat/completions` HTTP 200，说明设备侧 HTTPS 证书校验、Base URL、API Key、云端鉴权和最小文本 LLM 请求链路已打通。
- 用户提供的 Key 作为开发阶段项目 Key 使用；设备真正使用前直接替换，仍不要把 Key 明文写入源码或文档。

下一轮需要准备：

```text
已完成：GET https://api.deepseek.com/models -> HTTP 200
已完成：POST https://api.deepseek.com/chat/completions -> HTTP 200
API Key：开发阶段继续使用当前 DeepSeek Key，设备真正使用前替换
测试模型名：优先 deepseek-v4-flash，必要时兼容 deepseek-chat
已完成：API 配置迁移到 NVS/配网页面
已完成：REC TEST 录音缓冲验证
已完成：ASR TEST 返回 FIN_TEXT
已完成：VOICE LLM 半闭环入口和成功链路
已完成：TTS TEST 单项入口和上板播放验证
已完成：完整开发阶段凭据重刷并快速回归 VOICE LLM / TTS TEST
阶段边界：hardware_bringup 冻结为诊断工具
当前下一步：进入 XiaoClaw 正式集成
2026-05-14：XiaoClaw openclaw-v2.7 board profile、build/flash/boot、release 包和本地百度 TTS provider 基础接入已完成
当前下一步：XiaoClaw 百度 realtime ASR adapter 与正式 ASR->LLM->TTS 闭环
```
