# ESP32-S3 语音陪伴设备开发问题解决记录

更新时间：2026-06-01（第九版）

本文档专门记录开发过程中遇到的问题、定位过程、解决方法和最终结果。

## 记录格式

每个问题尽量按以下结构记录：

```text
问题现象：
原因定位：
解决方法：
验证结果：
后续注意：
```

### 问题 Y：短回复 TTS 播放时首字偶发被吞，根因是第一帧到达后切到 speaking 又重置了解码/播放队列

问题现象：
- 实机短回复场景下，偶发出现回复首字缺失
- 例如完整应为“现在上午10点”，实际听感会变成“在上午10点”
- 该现象在短回复里更明显，在“介绍一下你自己”“讲个故事”这类长回复中不明显

原因定位：
- 问题不在后端文本本身，也不是主因来自功放冷启动
- 客户端在收到第一帧 TTS binary 后，会从 `synthesizing` 切到 `speaking`
- 但 `kDeviceStateSpeaking` 的状态处理里又执行了一次 `audio_service_.ResetDecoder()`
- 此时首帧或首段 PCM 可能已经进入 `audio_decode_queue_` / `audio_playback_queue_`
- 二次 reset 会把这一小段起播音频清掉，因此短回复更容易表现为首字被吞

解决方法：
- 将 TTS 解码器/播放队列清理前移到 `OnTtsStart()`
- 删除 `kDeviceStateSpeaking` 入口中对新一轮 TTS 的二次 `ResetDecoder()`
- 这样旧播放状态仍会在新一轮开始前清理，但不会与首帧入队发生竞态

涉及文件：
- `xiaoclaw-ghproxycom/main/application.cc`
- `项目开发进度_XiaoClawBrain.md`
- `开发问题解决记录.md`

验证结果：
- `source /Users/mashiyue/esp/esp-idf-v5.5.2/export.sh && idf.py build`：通过
- `source /Users/mashiyue/esp/esp-idf-v5.5.2/export.sh && idf.py -p /dev/cu.usbmodem5B5E1028821 flash`：通过
- 实机针对性复测两轮：
  - `现在几点`
  - `今天星期几`
- 用户反馈：两轮都正常，未再出现首字丢失

后续注意：
- 清理旧播放链路应发生在 `tts_start` 或更早的边界，不要等第一帧到来后再在 `speaking` 入口清队列
- 如果后续再做 TTS 起播优化，可继续观察硬件输出暖机，但本次主因已确认是软件时序竞态

### 问题 X：本地唤醒后“聆听阶段很短”，根因是唤醒词本身被送进了下一轮 ASR

问题现象：
- 用户主观感受是：先说 `你好小智` 后，设备虽然会进入 `listening`，但还没来得及在提示音后说完问题，就很快结束聆听并开始回答
- 更具体地说，日志中常出现：
  - `识别文本: 你好小哥哥。`
  - `识别文本: 你好小猪。`
  - `识别文本: 你好，小智老师讲话。`
- 结果设备会把唤醒词本身当成聊天内容来回复，而不是等用户后续正式问题

原因定位：
- 这不是单纯的“聆听超时太短”
- 固件在本地唤醒后，会把 pre-wake ring buffer 和唤醒词残留音频一起带入后续 STREAM ASR
- 同时 XiaoClaw WS 路径还会主动上传 wake word opus 包
- 对后端来说，`detect` 之后马上收到的首批音频里已经包含唤醒词本身，于是会在用户真正开口提问前先完成一次识别

解决方法：
- 在 `xiaoclaw-ghproxycom/main/audio/audio_service.h/cc` 新增：
  - `DiscardPreWakeAudioOnNextFlush()`
- 策略调整为：
  - 本地唤醒命中后，先清空 pre-wake ring buffer，避免把唤醒前/唤醒词残留继续送进下一轮 ASR
  - XiaoClaw WS 路径下不再主动上传 wake word opus 包
  - 唤醒后只发 `listen detect` 控制消息，真正的问题等待提示音后再开始上传
- 同步修改：
  - `xiaoclaw-ghproxycom/main/application.cc`

涉及文件：
- `xiaoclaw-ghproxycom/main/audio/audio_service.h`
- `xiaoclaw-ghproxycom/main/audio/audio_service.cc`
- `xiaoclaw-ghproxycom/main/application.cc`
- `项目开发进度_XiaoClawBrain.md`
- `开发问题解决记录.md`

验证结果：
- `idf.py build`：通过
- `idf.py -p /dev/cu.usbmodem5B5E1028821 flash`：通过
- 实机按“先说 `你好小智`，等提示音，再说 `现在几点`”复测
- 最新后端日志确认：
  - `listen detect: 你好小智`
  - `listen start mode=auto`
  - `识别文本: 现在几点了？`
- 设备成功播报：
  - `现在是 17:34，今天是 2026-06-01，星期一`

后续注意：
- 如果当前产品交互采用“先唤醒、再提问”，就不应再把唤醒词残留音频送给后端作为问题主体
- 后续若要支持更自然的一口气连说 `你好小智，现在几点`，需要单独做体验优化，不能再直接复用这套“唤醒后剥离唤醒词音频”的策略

---

### 问题 W：Baidu STREAM ASR 只识别到半句时仍直接进 LLM，导致设备答非所问去讲故事

问题现象：
- 实机 BOOT 手动录音测试中，用户说的是“现在几点”
- 但后端日志显示实际识别结果只有：`现在。`
- 旧逻辑会把这类“非空但明显不完整”的短句直接送进 LLM
- 结果设备没有回答时间，而是自由发挥去讲故事

原因定位：
- `core/providers/asr/baidu.py` 与 `core/providers/asr/base.py` 原先只检查 `text_len > 0`
- 也就是说只有“空识别”才会走 `ASR_EMPTY`
- 像 `现在。`、`这个。` 这类半截短句虽然明显不够形成有效意图，但由于长度非零，仍会继续 `startToChat()`
- 项目规则里已有“单字/短词白名单保留，其他短句不要误进主链路”的边界，但代码未真正落地

解决方法：
- 在 `core/utils/util.py` 新增：
  - `ASR_SHORT_TEXT_WHITELIST`
  - `should_accept_asr_text()`
- 策略改为：
  - 识别文本为空：返回 `ASR_EMPTY`
  - 识别文本过短且不在白名单：同样返回 `ASR_EMPTY`
  - 仅当文本达到最小长度，或命中白名单短词（如 `继续`、`停止`）时，才允许进入 LLM
- 同步修改：
  - `core/providers/asr/base.py`
  - `core/providers/asr/baidu.py`
- 新增测试 `tests/test_asr_short_text_guard.py`，覆盖：
  - `现在。` 被拦截为 `ASR_EMPTY`
  - `继续` 等白名单短词仍可正常通过

涉及文件：
- `xiaozhi-esp32-server/main/xiaozhi-server/core/utils/util.py`
- `xiaozhi-esp32-server/main/xiaozhi-server/core/providers/asr/base.py`
- `xiaozhi-esp32-server/main/xiaozhi-server/core/providers/asr/baidu.py`
- `xiaozhi-esp32-server/main/xiaozhi-server/tests/test_asr_short_text_guard.py`
- `项目开发进度_XiaoClawBrain.md`
- `开发问题解决记录.md`

验证结果：
- `python3 -m unittest tests.test_asr_short_text_guard -v`：通过
- `python3 -m unittest tests.test_listen_message_handler -v`：通过
- 热更新运行中的 `xiaozhi-esp32-server` 容器并重启后，重新实测 BOOT 录音
- 新日志显示：
  - `识别文本: 现在几点了？`
  - 设备随后成功播报：`现在是 16:59，今天是 2026-06-01，星期一`

后续注意：
- 不要把“非空文本”直接等同于“有效用户意图”
- 对语音链路，除了空识别外，还要单独兜底“半截短句”这类弱识别结果
- 若后续继续优化唤醒词后的首词完整性，应优先减少首词被截断，而不是放宽短句进入 LLM 的门槛

---

### 问题 U：WakeNet 模型已正确选中，但 `model` 分区只有 256KB，实机烧录会覆盖 `fatfs`

问题现象：
- P8-9 代码验收通过后，准备实机烧录时发现两套构建结果不一致：
  - 直接 `idf.py build` 的 `build-openclaw-verify/srmodels/srmodels.bin` 只有 4 字节
  - 按板级配置链 `python3 scripts/release.py openclaw-v2.7 --name openclaw-v2.7` 构建后，`build/srmodels/srmodels.bin` 变为 **291042 bytes**
- 同时构建日志明确提示：`Recommended model partition size: 285K`
- 当前分区表里 `model` 只有 `0x40000`（256KB），小于实际模型包，若直接烧录到 `0x820000` 会覆盖后续 `fatfs`

原因定位：
- 直接 `idf.py build` 读取的是仓库根目录 `sdkconfig`，其中 `CONFIG_SR_WN_WN9_NIHAOXIAOZHI_TTS` 未启用，所以只打出了 4 字节空模型包
- 板级 `config.json` 通过 `release.py` 正常应用后，WakeNet 模型被真实打包进 `srmodels.bin`
- 但先前为 P8-9 新增的 `model` 分区只按“256KB 估算”配置，没有以真实构建产物大小做二次校验

解决方法：
- 用板级构建链验证最终配置，确认以下符号真实启用：
  - `CONFIG_USE_AFE_WAKE_WORD=y`
  - `CONFIG_SEND_WAKE_WORD_DATA=y`
  - `CONFIG_SR_WN_WN9_NIHAOXIAOZHI_TTS=y`
- 将 `partitions/openclaw_v2_7_16m.csv` 调整为：
  - `model, 0x820000, 0x50000`（320KB）
  - `fatfs` 起始偏移从 `0x860000` 顺延到 `0x870000`
- 这样可完整容纳当前 291042 字节的 `srmodels.bin`，并为后续小幅模型增量保留余量

涉及文件：
- `xiaoclaw-ghproxycom/partitions/openclaw_v2_7_16m.csv`
- `项目开发进度_XiaoClawBrain.md`
- `开发问题解决记录.md`

验证结果：
- `build/srmodels/srmodels.bin` 实际大小：`291042 bytes`
- 板级构建日志：`Recommended model partition size: 285K`
- `sdkconfig` 中确认：
  - `CONFIG_USE_AFE_WAKE_WORD=y`
  - `CONFIG_SEND_WAKE_WORD_DATA=y`
  - `CONFIG_SR_WN_WN9_NIHAOXIAOZHI_TTS=y`
- 实机烧录日志确认：
  - `Wrote 291044 bytes ... at 0x00820000`
  - `Hash of data verified`
- 启动日志确认：
  - 分区表中的 `model` 为 `00820000 00050000`
  - `MODEL_LOADER: Successfully load srmodels`
  - `AfeWakeWord: Model 0: wn9_nihaoxiaozhi_tts`

后续注意：
- 凡是新增 `model` / `assets` / `fatfs` 这类资源分区，都不能只凭估算值定尺寸；必须至少做一次“真实构建产物大小 vs 分区大小”的核对
- 对依赖 `config.json` 的板级功能，不能只用裸 `idf.py build` 验证；要优先走板级构建链，避免 `sdkconfig` 与 `config.json` 口径漂移
- 实机烧录前，先确认刷写参数里的 `srmodels.bin` 大小不超过 `model` 分区，避免静默覆盖后续分区

---

### 问题 V：OpenClaw 唤醒词已命中，但后续仍走旧 `protocol_`/MQTT 路径，导致“正在寻找可用服务”

问题现象：
- 实机日志已经确认：
  - `XiaoClawWS: ws connected`
  - `MODEL_LOADER: Successfully load srmodels`
  - `AfeWakeWord: Model 0: wn9_nihaoxiaozhi_tts`
- 说明板子已连上 XiaoClaw WebSocket，WakeNet 模型也已正确加载
- 但用户说出“你好小智”后，日志却出现：
  - `State: idle -> connecting`
  - `MQTT: MQTT is not connected, try to connect now`
  - `错误: 正在寻找可用服务`
- 表现为：唤醒词命中，但没有开始真正的 XiaoClaw 音频上传与回复

原因定位：
- `InitializeXiaoClawWsClient()` 是独立的 OpenClaw 运行时通道
- 但 `HandleWakeWordDetectedEvent()`、`ContinueWakeWordInvoke()`、`HandleStartListeningEvent()`、`HandleStopListeningEvent()`、`ContinueOpenAudioChannel()` 和 `HandleStateChangedEvent()` 的 `kDeviceStateListening` 分支里，仍有多处沿用老的 `protocol_`
- 当前 OTA 配置没有 MQTT endpoint，因此 `protocol_` 会回退到 `MqttProtocol`
- 结果就是：
  - XiaoClaw WS 已连通
  - 唤醒后实际控制却仍调用 `protocol_->OpenAudioChannel()` / `protocol_->SendStartListening()`
  - 最终掉进 MQTT fallback 并报“正在寻找可用服务”

解决方法：
- 在 `main/xiaoclaw_ws_client.h/cc` 新增：
  - `SendWakeWordDetectedJson(wake_word)`，对应 `listen.state = detect`
  - `SendListenStartJson(ListeningMode mode)`，支持 `manual/auto/realtime`
- 在 `main/application.h/cc` 新增并切换到 XiaoClaw WS 路径：
  - `IsXiaoClawWsReady()`
  - `XiaoClawSendWakeWordDetected()`
  - `XiaoClawBeginListening()`
- 修复后的 OpenClaw 唤醒链路：
  - wake word 命中后先发 `detect`
  - 本地状态进入 `wakeup_detected -> listening`
  - `listening` 状态下通过 `XiaoClawSendListenStart()` + `XiaoClawStartAudioUpload()` 开始上传
- 同时把手动 start/stop、`ContinueOpenAudioChannel()`、`WakeWordInvoke()` 等入口一并收口，避免其他入口再次回退到 MQTT

涉及文件：
- `xiaoclaw-ghproxycom/main/xiaoclaw_ws_client.h`
- `xiaoclaw-ghproxycom/main/xiaoclaw_ws_client.cc`
- `xiaoclaw-ghproxycom/main/application.h`
- `xiaoclaw-ghproxycom/main/application.cc`
- `项目开发进度_XiaoClawBrain.md`
- `开发问题解决记录.md`

验证结果：
- `source /Users/mashiyue/esp/esp-idf-v5.5.2/export.sh && idf.py build`：通过
- `source /Users/mashiyue/esp/esp-idf-v5.5.2/export.sh && idf.py -p /dev/cu.usbmodem5B5E1028821 flash`：通过
- 最新固件已重新烧录到设备，待复测最新日志是否已从 `MQTT fallback` 切换为 `listen detect/start` + `uploading_audio`

后续注意：
- OpenClaw 固件当前同时保留了通用 `protocol_` 和 `XiaoClawWsClient` 两条链路；凡是新增录音/唤醒/播放控制入口，都要先明确它属于哪条链路，避免“连接走 XiaoClaw，控制走 MQTT”的分裂状态
- 对 OpenClaw V2.7 这类运行时配置优先走 XiaoClaw WS 的板子，回归时应重点关注日志里是否再次出现 `MQTT is not connected`

---

### 问题 T：SkillRunner 受限环境缺少 TMPDIR，Darwin 上子进程持续打印 `confstr()` warning

问题现象：
- 所有通过 `SkillRunner` 拉起 Python 子进程的测试虽然能通过，但 `stderr` 会持续出现：
  - `python3: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead`
- 这类噪声不会导致失败，但会污染回归输出，让后续开发很难快速识别真正的异常日志

原因定位：
- `core/skills/skill_runner.py` 的 `_build_limited_env()` 为了遵守最小权限原则，只给 skill 子进程保留了 `PATH`
- 在 macOS / Darwin 上，`python3` 启动时会尝试通过 `DARWIN_USER_TEMP_DIR` 相关机制解析用户临时目录；当环境里既没有 `TMPDIR`，又取不到用户临时目录时，就会退回 `/tmp` 并打印 warning
- 这不是 skill 脚本自身的问题，而是受限运行环境缺少基础临时目录变量

解决方法：
- 在 `_build_limited_env()` 中补齐非敏感的基础临时目录变量：
  - `TMPDIR`
  - `TMP`
  - `TEMP`
- 取值优先使用宿主环境 `TMPDIR`，否则回退 `tempfile.gettempdir()`
- 新增单测 `test_limited_env_includes_temp_vars`，确保这几个变量始终存在且一致

涉及文件：
- `main/xiaozhi-server/core/skills/skill_runner.py`
- `main/xiaozhi-server/tests/test_skill_runner.py`
- `项目开发进度_XiaoClawBrain.md`
- `开发问题解决记录.md`

验证结果：
- `python3 -m unittest tests.test_skill_runner -v`：21/21 通过
- `python3 -W error::ResourceWarning -m unittest tests.test_skill_global -v`：17/17 通过
- `python3 -W error::ResourceWarning -m unittest discover -s tests -v`：279/279 通过
- 原先的 `DARWIN_USER_TEMP_DIR` warning 不再出现

后续注意：
- “limited env” 只是不透传敏感变量，不代表连基础运行时变量都要清空；像临时目录这类非敏感但影响稳定性的变量，应在 runner 默认环境里显式给出
- 以后只要新增受限子进程运行链路，都优先补一个“基础环境最小必需项”检查，避免把平台 warning 留到后期收尾

---

### 问题 S：P8 相关 startToChat 测试遗留未关闭 event loop / socket 警告

问题现象：
- `python3 -m unittest discover -s tests` 虽然 278/278 通过，但进程退出时仍打印 `ResourceWarning: unclosed event loop` 和 `unclosed <socket.socket ...>`
- 定向到 `tests.test_skill_matcher` / `tests.test_skill_global` 后，在 `-W error::ResourceWarning` 模式下可以稳定复现

原因定位：
- `tests/test_skill_matcher.py` 和 `tests/test_skill_global.py` 的 `setUpClass()` 在没有默认 loop 时会 `asyncio.set_event_loop(asyncio.new_event_loop())`，但没有配套 `tearDownClass()` 关闭
- 两个文件的 `_make_conn()` 又给每个假连接创建了 `conn.loop = asyncio.new_event_loop()`，测试结束后也没有显式 `close()`
- 这些 loop 挂着底层 socket，到解释器退出阶段才被垃圾回收，因此在全量收尾时集中冒出 warning

解决方法：
- 两个测试文件都补齐：
  - `setUpClass()` 保存自建 import loop
  - `tearDownClass()` 关闭该 loop，并 `asyncio.set_event_loop(None)`
  - `_make_conn()` 创建的 `conn.loop` 通过 `self.addCleanup(conn.loop.close)` 回收

涉及文件：
- `main/xiaozhi-server/tests/test_skill_matcher.py`
- `main/xiaozhi-server/tests/test_skill_global.py`
- `项目开发进度_XiaoClawBrain.md`
- `开发问题解决记录.md`

验证结果：
- `python3 -W error::ResourceWarning -m unittest tests.test_skill_matcher -v`：37/37 通过
- `python3 -W error::ResourceWarning -m unittest tests.test_skill_global -v`：17/17 通过
- `python3 -W error::ResourceWarning -m unittest discover -s tests -v`：278/278 通过，不再出现 `unclosed event loop` / `unclosed socket`

后续注意：
- 凡是测试里手工创建 event loop，都要明确写出归属与关闭点；不要假设进程退出会帮忙回收
- 像 `conn.loop` 这种挂在测试桩对象上的 loop，最稳妥就是在创建处立刻 `addCleanup(...)`

---

### 问题 R：SkillImporter 放过包内 symlink，导致本地导入递归拷贝失败

问题现象：
- P8-7 已通过主验收，但追加清尾时发现：如果本地 skill 目录内包含“指向包内目录”的 symlink，例如 `selflink -> .`，`import_from_local()` 不会拦截
- 随后 `shutil.copytree(..., symlinks=False)` 会沿着 symlink 递归跟随，最终报 `Too many levels of symbolic links`，表现成一个含糊的 `SOURCE_NOT_FOUND`

原因定位：
- `core/skills/skill_importer.py` 旧版 `_check_unsafe_symlinks()` 只拒绝“resolve 后落到 skill 根目录外”的 symlink
- 指向 skill 根目录内的 symlink 会被当成“安全”放过
- 但导入复制阶段使用的是 `copytree(..., symlinks=False)`，它会跟随 symlink 内容而不是保留链接本身，因此循环 symlink 会触发递归拷贝问题

解决方法：
- 将 `_check_unsafe_symlinks()` 收紧为：发现任意 symlink 就拒绝导入，并返回 `UNSAFE_SYMLINK`
- 新增回归测试 `test_local_import_rejects_internal_symlink_loop`
- 这样本地目录导入与 tar 远程归档导入的安全口径都收紧到“不接受链接对象”

涉及文件：
- `main/xiaozhi-server/core/skills/skill_importer.py`
- `main/xiaozhi-server/tests/test_skill_importer.py`
- `项目开发进度_XiaoClawBrain.md`
- `开发问题解决记录.md`

验证结果：
- `python3 -m unittest tests.test_skill_importer -v`：39/39 通过
- `python3 -W error::ResourceWarning -m unittest discover -s tests -v`：278/278 通过

后续注意：
- 以后凡是要“复制第三方包目录”的链路，都不要把“包内 symlink”默认当成安全对象；即使目标没出目录，也可能触发递归跟随或复制放大
- 如果后续 P8-8 需要做 pending → enabled 搬运，也要沿用这条“拒绝任何 symlink/hardlink”的导入边界

---

### 问题 P：SkillRunner 大量 stderr 被误判为 `SKILL_TIMEOUT`

### 问题 Q：SkillMatcher 显式前缀会把 Skill 参数错误转成小写

问题现象：
- P8-5 表面验收已经通过，但追加 review 时发现：显式前缀命中后，传给 skill 的 `args_text` 会被整体转成小写
- 例如 `运行技能 echo Hello JSON` 最终执行成 `echo: hello json`
- 中文样例和现有 35 项测试都能通过，因此这个问题会被绿灯掩盖

原因定位：
- `core/skills/skill_matcher.py` 的 `_try_prefix_parse()` 先对整句 `utterance` 调 `_normalize()`，得到全小写字符串
- 后续 `args_text` 直接从这个归一化后的字符串切分出来，导致 skill 名匹配虽然正确，但参数原文丢失

解决方法：
- 将显式前缀解析拆成“匹配用归一化字符串”和“透传用原始字符串”两套数据：
  - skill 名仍使用归一化后的 `raw_name` 做不区分大小写匹配
  - `args_text` 改为从原始 `utterance.strip()` 中切分，保留用户输入的原始大小写和内容
- 追加两条回归测试：
  - `test_prefix_args_preserve_original_case`
  - `test_run_echo_skill_preserves_original_case_args`

涉及文件：
- `main/xiaozhi-server/core/skills/skill_matcher.py`
- `main/xiaozhi-server/tests/test_skill_matcher.py`
- `项目开发进度_XiaoClawBrain.md`
- `开发问题解决记录.md`
- `START_HERE_项目速览.md`
- `P8完整方案_自定义Skill与Agent循环_XiaoClawBrain.md`

验证结果：
- `python3 -m unittest tests.test_skill_matcher.TestSkillMatcherExplicitPrefix.test_prefix_args_preserve_original_case -v`：1/1 通过（修复前先红灯）
- `python3 -m unittest tests.test_skill_matcher -v`：37/37 通过
- `python3 -m unittest discover -s tests -v`：188/188 通过（0 回归）

后续注意：
- 后续凡是“匹配逻辑需要归一化、执行逻辑需要保真”的链路，都不要复用同一份归一化字符串做参数透传
- SkillMatcher 的测试以后不要只测中文参数，还要至少覆盖一条英文/大小写敏感参数，防止再次被样例掩盖

---

### 问题 P：SkillRunner 大量 stderr 被误判为 `SKILL_TIMEOUT`

问题现象：
- P8-2 原始验收项全部通过，但追加复核时发现：只要 skill 持续向 `stderr` 输出较多内容，runner 就会在 timeout 到达时返回 `SKILL_TIMEOUT`
- 即使 skill 最终会正常向 `stdout` 输出合法 JSON，也会被误判成超时

原因定位：
- `core/skills/skill_runner.py` 旧版 `_communicate_with_limit()` 先串行读完 `stdout`，最后才一次性 `read()` `stderr`
- 当 skill 写入大量 `stderr` 时，`stderr` pipe 会先被写满，子进程阻塞在写日志阶段，导致 `stdout` 迟迟无法完成
- 外层 `asyncio.wait_for(...)` 看到的是整体通信卡住，于是误报为 `SKILL_TIMEOUT`

解决方法：
- 将 `stdout` / `stderr` 改为并发 drain：
  - 新增 `_read_stdout_with_limit()` 负责 stdout 限流
  - 新增 `_drain_stderr()` 持续读取 stderr，避免 pipe 背压
  - `_communicate_with_limit()` 改为 `asyncio.create_task(...) + asyncio.gather(...)`
- 新增 `_STDERR_CAPTURE_LIMIT = 65536`，只保留有限 stderr 片段用于日志，避免无限放大内存
- 在 timeout 分支增加 `try/except ProcessLookupError`，避免和已退出子进程发生双 kill 竞争
- 新增回归测试 `test_large_stderr_does_not_fake_timeout`

涉及文件：
- `main/xiaozhi-server/core/skills/skill_runner.py`
- `main/xiaozhi-server/tests/test_skill_runner.py`
- `项目开发进度_XiaoClawBrain.md`
- `开发问题解决记录.md`
- `START_HERE_项目速览.md`

验证结果：
- `python3 -m unittest tests.test_skill_runner -v`：20/20 通过
- `python3 -m unittest discover -s tests`：123/123 通过

后续注意：
- 以后凡是子进程同时使用 `stdout` / `stderr` pipe，都不要用“先读一路，后读另一路”的串行模式
- 与 timeout 相关的修复必须补带压测性质的回归用例，避免只覆盖“安静脚本”场景

---

### 问题 O：SkillDiscovery 未接入 `skills.paths` / `allow_home_agents` 配置语义

问题现象：
- P8-1 的扫描、冲突、pending 逻辑都能跑通，但验收时发现 `SkillDiscovery` 无论配置怎么改，都只扫描硬编码默认路径
- 即使 `skills.allow_home_agents: false`，`~/.agents/skills` 仍然会被扫描并注册

原因定位：
- `core/skills/skill_discovery.py` 把 `SCAN_PATHS` / `PENDING_PATHS` 写死在类常量里，`scan_all()` 直接遍历这些常量
- `scan_all()` 没有读取 `config.yaml` 的 `skills.paths.*` 和 `allow_home_agents`
- 因此自定义路径失效，home 扫描开关也失效

解决方法：
- 将默认扫描源改成“键名 + 默认路径 + 优先级”的结构
- 新增按配置展开扫描源的辅助方法：
  - 普通路径从 `skills.paths` 读取，缺省时回退默认值
  - pending 路径从 `skills.paths.pending` 读取
  - `allow_home_agents: false` 时跳过 `home_agents`
  - `skills.enabled: false` 时直接返回空结果
- 新增两组回归测试：
  - 自定义 `skills.paths.*` 生效
  - `allow_home_agents: false` 时不扫描 home skills

涉及文件：
- `main/xiaozhi-server/core/skills/skill_discovery.py`
- `main/xiaozhi-server/tests/test_skill_discovery.py`
- `项目开发进度_XiaoClawBrain.md`
- `开发问题解决记录.md`

验证结果：
- `python3 -m unittest tests.test_skill_discovery`：19/19 通过
- `python3 -m unittest tests.test_skill_manifest`：24/24 通过
- `python3 -m unittest discover -s tests`：103/103 通过

后续注意：
- 后续 P8-2/P8-3 不要绕开 `skills` 配置结构直接写死路径或开关
- 以后凡是配置项先在 `config.yaml` 冻结过，就要补“非默认配置值也生效”的回归测试，避免只测默认路径

---

### 问题 N：测试尾项存在未关闭 event loop 与重复测试类覆盖

问题现象：
- `python3 -m unittest discover -s tests` 虽然 84/84 通过，但进程退出时仍打印 `ResourceWarning: unclosed event loop` 和 `unclosed <socket.socket ...>`
- `tests/test_skill_manifest.py` 中 `TestMetadataOnlyDetection` 被定义了两次，前一个同名类会被后一个覆盖，形成死测试定义

原因定位：
- `tests/test_tts_start_order.py` 的 `ChatLlmTimeoutTest` 使用 `asyncio.new_event_loop()` + 后台线程 `run_forever()`，测试结束时只 `loop.stop()` 和 `join()`，没有在所属线程内 `close()`
- `tests/test_skill_manifest.py` 在补回归测试时重复粘贴了同名测试类；Python 解释器加载模块时以后定义为准，前一个类不会参与执行

解决方法：
- 将 `tests/test_tts_start_order.py:_run_loop()` 改为 `try/finally` 结构，在 `run_forever()` 返回后统一执行 `loop.close()`，并清理线程内 event loop 绑定
- 删除 `tests/test_skill_manifest.py` 中被覆盖的重复 `TestMetadataOnlyDetection` 定义，保留包含回归用例的最终版本
- 在相关测试文件入口定向忽略本机 Python 3.9 + LibreSSL 组合触发的 `urllib3.exceptions.NotOpenSSLWarning`，避免环境噪声污染回归输出

涉及文件：
- `main/xiaozhi-server/tests/test_tts_start_order.py`
- `main/xiaozhi-server/tests/test_skill_manifest.py`
- `main/xiaozhi-server/tests/test_baidu_token.py`
- `main/xiaozhi-server/tests/test_baidu_tts_last.py`
- `main/xiaozhi-server/tests/test_config_secrets.py`
- `main/xiaozhi-server/tests/test_fast_path.py`
- `项目开发进度_XiaoClawBrain.md`
- `开发问题解决记录.md`

验证结果：
- `python3 -W error::ResourceWarning -m unittest tests.test_tts_start_order.ChatLlmTimeoutTest -v`：4/4 通过，不再出现 `unclosed event loop` / `unclosed socket`
- `python3 -m unittest tests.test_skill_manifest`：24/24 通过
- `python3 -m unittest discover -s tests`：84/84 通过

后续注意：
- 以后凡是测试里手工创建 `asyncio` event loop，都要在所属线程内负责 `close()`，不能只 `stop()`
- 新增回归测试后，注意检查是否有同名 `TestCase` 被重复定义覆盖，避免“看起来在测，实际上没执行”的假覆盖

---

### 问题 K：BaiduTTS `_process_remaining_text_stream(is_last=True)` 未产生音频 LAST

问题现象：
- LLM 回复自然收尾时，`tts stop` 永不发送，设备卡在 `speaking`，35 秒后由 `ROUND_TIMEOUT` 强制结束
- 根因链：`_process_remaining_text_stream(is_last=True)` 在剩余文本非空时只调 `to_tts_single_stream`（产生 `SentenceType.MIDDLE`），没有调 `_process_before_stop_play_files()` → 音频队列 LAST 未产生 → `sendAudioMessage` 不调 `send_tts_message("stop")` → `tts stop` 永不发送 → 设备卡 `speaking`

原因定位：
- `baidu.py:89-110`：`_process_remaining_text_stream` 的 `is_last=True` 逻辑仅在 `remaining_text` 为空或 `segment_text` 为空（纯标点）时调 `_process_before_stop_play_files()`，在有实际剩余文本要合成时跳过了 LAST 收口

解决方法：
- 重构 `_process_remaining_text_stream`：始终在 `is_last=True` 时无脑调 `_process_before_stop_play_files()`
- 修复 `processed_chars += len(full_text)` → `processed_chars = len(full_text)`（累加 bug）
- `text_to_speak` 的 `client_abort` 从 `continue` 改为 `break`（提前结束百度流）
- `text_to_speak` 在 `client_abort` 后清空 `pcm_buffer`，并禁止把 abort 前残留的半截 PCM flush 到 Opus 队列
- 补充方法级单测 `test_baidu_tts_last.py`

涉及文件：
- `core/providers/tts/baidu.py`
- `tests/test_baidu_tts_last.py`

验证结果：
- `python3 -m unittest tests.test_baidu_tts_last`：3/3 通过（TDD 先红灯后绿灯）
- `python3 -m unittest discover -s tests`：29/29 通过
- `docker compose -f docker-compose_all.yml build xiaozhi-esp32-server`：通过
- ESP32 `idf.py build`：通过

后续注意：
- TTS 文本 LAST → 音频 LAST → `tts stop` 链路无需再单独补测
- `text_to_speak` 的 `client_abort break` 可避免 abort 后在百度 HTTP 流上继续浪费响应 chunk，并避免残留 PCM 污染后续音频队列

---

### 问题 L：长回复请求被 DeepSeek `max_tokens: 120` 截成半句

问题现象：
- 实机请求“讲一个很长很长的故事”时，后端未触发 `ROUND_TIMEOUT` / `TTS_TIMEOUT`，而是在 3 秒内主动发出 `SentenceType.LAST`
- 实际播报停在半句“结果帮他打出了一行”，用户听感为被截断

原因定位：
- 后端日志显示自然进入 `playback drain phase started`，说明播放收口链路正常
- 默认 `config.yaml` 中 `LLM.DeepSeekLLM.max_tokens: 120` 是硬上限；本机私有配置只覆盖 key/model/url，未覆盖 `max_tokens`，因此运行时继承 120，长故事会被模型输出上限截断

解决方法：
- 将默认 `LLM.DeepSeekLLM.max_tokens` 从 `120` 调整为 `500`
- 保留 `voice_reply_budget` 软提示，仍让普通语音回复尽量简短，但允许用户明确要求长回答时生成完整内容
- 新增配置回归测试，要求 DeepSeek 默认 `max_tokens >= 500`

涉及文件：
- `main/xiaozhi-server/config.yaml`
- `main/xiaozhi-server/tests/test_config_secrets.py`
- `项目开发进度_XiaoClawBrain.md`
- `开发问题解决记录.md`

验证结果：
- `python3 -m unittest tests.test_config_secrets`：3/3 通过（TDD 先红灯后绿灯）
- `python3 -m unittest discover -s tests`：30/30 通过
- 实机复测：再次请求“讲一个很长很长的故事”后生成明显更长内容，不再停在半句；后端自然发送 `SentenceType.LAST` 并进入 `playback drain phase started`
- 播放中短按 BOOT：后端收到 `{"type":"abort"}`，后续继续收到 ping，无 `ROUND_TIMEOUT` / `TTS_TIMEOUT` 尾巴

后续注意：
- 这不是播放被 35 秒切断，而是 LLM 输出预算硬截断
- 后续如需更细体验，可做“按意图动态语音预算”：普通问题保持短答，故事/解释/教程类请求自动放宽预算

---

### 问题 J：DeepSeek API Key 被误写入默认 config.yaml

问题现象：
- 默认 `main/xiaozhi-server/config.yaml` 中出现真实形态的 DeepSeek API Key
- 这违反项目“凭据只能写 SET/EMPTY 或空模板，真实值放私有配置”的安全约束

原因定位：
- DeepSeek 私有 Key 应放在 `main/xiaozhi-server/data/.config.yaml`
- 当前配置加载器会递归合并默认配置与 `data/.config.yaml`，私有配置优先，因此默认配置不需要保存真实 Key

解决方法：
- 将默认 `LLM.DeepSeekLLM.api_key` 改回空值模板
- 保持本机私有 `data/.config.yaml` 中的 DeepSeek 配置作为运行时覆盖来源
- 新增 `tests/test_config_secrets.py`，验证默认 DeepSeek 配置不含真实形态 API Key，并验证私有配置覆盖机制可用

涉及文件：
- `main/xiaozhi-server/config.yaml`
- `main/xiaozhi-server/tests/test_config_secrets.py`
- `项目开发进度_XiaoClawBrain.md`
- `开发问题解决记录.md`

验证结果：
- `python3 -m unittest tests.test_config_secrets`：通过
- 配置合并后 `LLM.DeepSeekLLM.api_key` 运行态为 `SET`
- `main/xiaozhi-server/data/.config.yaml` 已由 `.gitignore` 忽略

后续注意：
- 默认配置、文档、日志中不得再出现真实 API Key、Secret、Token
- 若该 Key 已暴露在历史记录或备份中，建议在 DeepSeek 控制台轮换 Key

### 问题 I：百度短语音 REST ASR 返回 3302，但实时 ASR WebSocket 可用

问题现象：
- 使用同一组百度智能云语音凭据做连通测试时，OAuth 可正常返回 token
- TTS 可正常返回 16k PCM 音频
- 短语音 REST ASR 使用 `dev_pid=1537/80001/1737` 均返回 `3302 No permission to access data`
- 用户提供百度实时语音识别 WebSocket 官方文档后，用实时接口测试返回 `FIN_TEXT err_no=0`

原因定位：
- 前一次测试用错了产品线：短语音 REST `server_api` 走 OAuth token 和另一组 pid 权限
- 百度实时 ASR 文档使用 WebSocket，START 帧里直接传 `appid + appkey + dev_pid=15372`
- 当前应用的实时语音识别权限可用，但短语音 REST 权限不一定可用

解决方法：
- `BaiduASR` 改为实时 WebSocket 实现，默认 `dev_pid=15372`
- `BaiduTTS` 保持 HTTP 流式接收 PCM chunk 并转 Opus 入队
- 本机私有配置已切换为 `selected_module.ASR = BaiduASR`、`selected_module.TTS = BaiduTTS`
- 实机首轮复测发现批量喂实时 ASR 时 5s watchdog 会截断；非流式批量模式下改为 `send_interval=0`，不再模拟实时 sleep
- 批量模式二次实机复测仍触发 5s `ASR_TIMEOUT`，因此改为真正 `InterfaceType.STREAM`
- STREAM 模式在第一帧音频到达时建立百度实时 ASR WebSocket，录音过程中边解 Opus 边推 PCM，stop 时发送 `FINISH`
- listen stop 后立即发送 `recognizing` 状态并启动 round timer，避免状态机停在上传阶段

涉及文件：
- `core/providers/baidu_token.py`
- `core/providers/asr/baidu.py`
- `core/providers/tts/baidu.py`
- `core/handle/textHandler/listenMessageHandler.py`
- `config.yaml`
- `tests/test_baidu_token.py`

验证结果：
- 百度 OAuth：通过
- 百度 TTS：通过
- 百度短语音 REST ASR：返回 `3302`
- 百度实时 ASR WebSocket：通过，返回 `FIN_TEXT err_no=0`
- `python3 -m unittest discover -s tests`：24 个测试通过
- 容器内 `selected_tts=BaiduTTS`，`selected_asr=BaiduASR`，`asr_dev_pid=15372`
- 容器内百度 TTS provider 短文本冒烟测试通过
- 首轮实机进入 `BaiduASR` 后触发 `ASR_TIMEOUT`，已调整发送间隔并等待继续实机复测
- 二次实机仍触发 `ASR_TIMEOUT`，已切换为 STREAM 版并重启
- 容器内确认 `BaiduASR interface=InterfaceType.STREAM`
- `python3 -m unittest discover -s tests`：24 个测试通过

后续注意：
- 百度凭据只放本机 `data/.config.yaml`，不得写入源码、默认配置、提交文档或日志
- 后续不要再用短语音 REST 的 `3302` 判断实时 ASR 权限；实时链路应按 WebSocket 文档单独验证
- 真正 `InterfaceType.STREAM` 版本应在录音过程中推帧，避免录音结束后再一次性发送导致 ASR timeout 压力

### 问题 H：TTS 已生成音频播放 drain 被 35s round_timeout 误判为整轮超时

问题现象：
- 实机复测中 LLM 已经有首包、TTS 也已经开始播放，但长回复仍可能在 35s 左右收到 `tts stop -> ROUND_TIMEOUT -> idle`
- 用户感知不是“没有生成”，而是“已经开始说了但尾部被硬截断”

原因定位：
- 旧逻辑把 ASR/LLM/TTS 生成阶段和已生成音频的播放发送 drain 共用同一个 35s `round_timeout`
- 当文本已经转成音频并进入发送队列后，后端仍按 round deadline 截断，容易把正常播放耗时误判为整轮卡死

解决方法：
- TTS 生成完成、准备发送 `tts stop` 时，如果本轮未 timeout，先清理 round watchdog
- 新增 `playback_drain_timeout`，仅用于等待已生成音频按实时播放节奏发完
- 播放队列 drain 自身超时后发送 `TTS_TIMEOUT -> idle`，并重置音频流控队列，避免尾巴继续下发
- 新增本地 fast path：确定性的“现在几点/当前时间”问题直接本地回复，天气/新闻继续走工具或 LLM

涉及文件：
- `config.yaml`
- `core/handle/sendAudioHandle.py`
- `core/handle/receiveAudioHandle.py`
- `core/handle/fastPath.py`
- `tests/test_tts_start_order.py`
- `tests/test_fast_path.py`

验证结果：
- `python3 -m unittest discover -s tests`：22 个测试通过
- `PYTHONPYCACHEPREFIX=/private/tmp/xcb_pycache python3 -m py_compile core/connection.py core/handle/sendAudioHandle.py core/handle/receiveAudioHandle.py core/handle/fastPath.py tests/test_tts_start_order.py tests/test_fast_path.py`：通过

后续注意：
- `ROUND_TIMEOUT` 主要用于生成链路或整轮 watchdog；播放发送队列自己的异常优先归到 `TTS_TIMEOUT`
- 百度流式 ASR/TTS 接入时也要保留这个分层，不要让云端 TTS 的播放 drain 再回到 35s round deadline

### 问题 G：LLM 进入 thinking 后无有效首包时仍退化为 ROUND_TIMEOUT

问题现象：
- 连续实机复测中，「用两句话解释一下量子计算。」ASR 正常出文本，设备进入 `thinking`
- 后端没有在 20s 左右发送 `LLM_TIMEOUT`
- 最终由 35s round watchdog 触发 `ROUND_TIMEOUT`
- 设备侧状态为 `thinking -> error -> idle`，用户感知为“最后两个问题没有反应”

原因定位：
- `_timeout_iter()` 只保护同步生成器单次 `next()` 的阻塞
- 实机中仍存在 LLM streaming 线程未及时把 timeout 抛回 `chat()` 的路径
- 因此 `chat()` 没有机会调用 `_fail_llm_timeout()`，只能等 35s `ROUND_TIMEOUT`

解决方法：
- `ConnectionHandler` 新增独立 LLM 有效 chunk watchdog
- 进入 LLM streaming 后记录 `llm_last_effective_at`
- 只有有效文本、工具调用或 reasoning chunk 才刷新 watchdog
- 20s 内没有有效 chunk 时，watchdog 在事件循环中主动调用 `_fail_llm_timeout()`，发送 `state=error -> LLM_TIMEOUT -> state=idle`
- 保留 `_timeout_iter()` per-chunk timeout 和 35s round watchdog 作为原有兜底

涉及文件：
- `core/connection.py`
- `tests/test_tts_start_order.py`

验证结果：
- `python3 -m unittest discover -s tests`：17 个测试通过
- `PYTHONPYCACHEPREFIX=/private/tmp/xcb_pycache python3 -m py_compile core/connection.py tests/test_tts_start_order.py`：通过
- 后端镜像已重建并重启，容器 healthy
- 后修补后的针对性实机复测已补跑：「用两句话解释一下量子计算。」在 `14:34:31` ASR 出文本，`14:34:44` 发送第一段 TTS，说明该轮不再是 LLM 无有效首包卡死
- 该轮仍因回复/TTS 偏长在 `14:35:06` 触发 TTS 队列等待超时和 `ROUND_TIMEOUT`，已额外收紧语音预算配置：80 字、2 句、25 秒、`DeepSeekLLM.max_tokens: 120`

后续注意：
- 以后判断 LLM timeout 不能只看生成器是否返回，还要看是否产生“有效首包/有效 chunk”
- 工具调用轮要继续覆盖空 chunk、递归 chat 和工具返回后 final answer 三类场景
- 若实机仍出现 `thinking` 长时间无响应，应优先检查 LLM watchdog 是否启动、是否被过早清理、以及 `round_timeout_reported` 是否提前置位
- 若实机有首包但仍在 35s 收口，应优先继续收紧语音预算或在进入 TTS 队列前做句子级裁剪，不要放宽 ESP32 状态机

### 问题 F：递归 chat(depth>0) 的 LLM timeout 漏口，未走显式错误协议

问题现象：
- 工具调用后的递归 chat(depth>0) 遇到 `LLMChunkTimeoutError` 时，`except` 处理块只会在 `depth == 0` 时调用 `_fail_llm_timeout()`
- depth>0 时直接 `return`，不发送 `state=error → LLM_TIMEOUT → state=idle`
- 客户端收不到 LLM_TIMEOUT，最终退化为 35s ROUND_TIMEOUT

原因定位：
- `core/connection.py` 1349-1354 行 `except LLMChunkTimeoutError` 的 `if depth == 0:` 守卫导致

解决方法：
- 移除 `if depth == 0:` 守卫，任何深度都调用 `_fail_llm_timeout()`
- `_fail_llm_timeout()` 本身幂等（检查 `round_timed_out` / `round_timeout_reported`），重复调用安全
- `asyncio.run_coroutine_threadsafe` 返回的 future 通过 `future.result(timeout=5)` 同步等待，确保协议发出后才返回

涉及文件：
- `core/connection.py`：`except LLMChunkTimeoutError` 处理块

验证结果：
- 全部 15 个单元测试通过（含 2 个新增：顶层 + 递归 chat 协议收口测试）
- `state=error → LLM_TIMEOUT → state=idle` 序列在两种场景下均正确发送

后续注意：
- 任何涉及 `LLMChunkTimeoutError` 的异常路径都要统一走 `_fail_llm_timeout()`，不应存在未覆盖的退出点
- 如果未来增加新的递归入口，同样需要保证 timeout 覆盖

### 问题 E：后端 LLM 调用缺乏超时保护，round_timeout 有覆盖盲区

问题现象：
- 实机测试中 DeepSeek API 卡死超过 3 分钟，后端无任何输出
- LLM timeout（20s）和 round timeout（35s）均未触发
- 设备长期等待，不回到 idle

原因定位：
1. `connection.py` 中 `self.llm.response()` 是同步生成器，HTTP 请求在首次 `next()` 时发出
2. `round_timeout` 检查仅在流式循环内部，首包前卡住则永远到不了检查点
3. `openai.py` 默认 httpx 超时 300s，与项目规范的 LLM timeout（20s）不符
4. `_timeout_iter` 早期实现使用累计预算（total deadline），不是 per-chunk 超时，且超时后静默 break 不发送错误协议

解决方法：
1. `config.yaml` 中为 `DeepSeekLLM` 增加 `timeout: {connect:3, write:5, read:20, pool:2}`
2. `openai.py` 默认 httpx timeout 从 300s 收紧到 20s
3. `connection.py` 中 `_timeout_iter` 改为每次 `next()` 独立计时 timeout_seconds，超时抛出 `LLMChunkTimeoutError`
4. `chat()` 捕获 `LLMChunkTimeoutError` 后调用 `_fail_llm_timeout()`，显式发送 `state=error → LLM_TIMEOUT → state=idle`
5. `start_round_timer()` 创建独立 `_round_watchdog_async()`，流循环进不去时 35s 仍能触发 `ROUND_TIMEOUT`

验证结果：
- 单元测试 15/15 通过（含 `chat()` 顶层/递归 timeout 协议收口测试）
- `PYTHONPYCACHEPREFIX=/private/tmp/xcb_pycache python3 -m py_compile core/connection.py core/providers/llm/openai/openai.py tests/test_tts_start_order.py` 通过
- 递归 `chat(depth>0)` 命中 timeout 时，客户端同样收到 `state=error → LLM_TIMEOUT → state=idle`

后续注意：
- 任何涉及 `LLMChunkTimeoutError` 的路径都应统一走 `_fail_llm_timeout()`，避免退化成静默 return 或错误落到 `ROUND_TIMEOUT`
- 如果未来新增新的递归入口或新的 provider 适配器，也要同步验证 `LLM_TIMEOUT` 协议覆盖

## 15. 长回复虽有 35 秒硬 timeout 但普通回复仍容易靠截断收尾

问题现象：

- 后端已能在单轮超过 35 秒时丢弃剩余 TTS 待发队列，并发送 `tts stop`、`ROUND_TIMEOUT`、`state=idle`。
- 但这只是最后保险丝；如果 LLM 默认输出过长，用户仍可能经常听到被硬 timeout 打断的回复，体验不够自然。

原因定位：

- 现有后端主要在 TTS 发送尾部做 35 秒硬收口，LLM 生成前没有明确“语音短回复”预算。
- 当前 `OMLXLLM` 使用 `type: ollama` 的 OpenAI-compatible chat 接口，但 Ollama provider 未读取和传递 `max_tokens`，无法从生成层限制输出长度。
- 若直接在 TTS 后端截断，流式内容可能已经进入 TTS 队列，容易出现半句话或不自然收尾。

解决方法：

- 新增配置项 `voice_reply_budget`：
  - `enabled: true`
  - `target_speech_seconds: 30`
  - `max_chinese_chars: 120`
  - `max_sentences: 3`
  - `overflow_hint: "我先讲到这里，想继续听你再问我。"`
- `ConnectionHandler` 新增语音预算 helper，在每轮 LLM 调用前临时追加 system 约束，要求回复适合语音播放、默认 1-3 句、中文约 120 字以内、不展开长列表、自然完整收尾。
- 该约束只加入本轮 `llm_dialogue`，不写回历史 `self.dialogue`。
- `OMLXLLM` 配置新增 `max_tokens: 180`。
- `core/providers/llm/ollama/ollama.py` 读取 `max_tokens` 并传入普通 streaming 与 `response_with_functions` 请求。
- 保留原有 35 秒单轮硬 timeout，继续作为异常兜底。

验证结果：

```text
python3 -m unittest discover -s tests
PYTHONPYCACHEPREFIX=/private/tmp/xcb_pycache python3 -m py_compile core/connection.py core/providers/llm/ollama/ollama.py tests/test_tts_start_order.py
```

后续注意：

- 本次没有修改协议字段、状态机或 ESP32 端逻辑。
- TTS 音频仍必须走 WebSocket binary frame，禁止恢复 base64 JSON 音频。
- 如果实机发现模型仍常超长，下一步再考虑流式句子边界裁剪；裁剪必须在内容进入 TTS 队列前进行，不能等完整生成后再处理。

## 14. 长回复场景下 TTS stop 延迟导致设备长时间停在 speaking

问题现象：

- BOOT 主链路状态顺序已恢复为 `recognizing -> thinking -> synthesizing -> speaking -> idle`。
- 长回复测试中，设备进入 `speaking` 后本地音频输出一度关闭，但后端仍继续下发大量 TTS binary frame。
- ESP32 必须等后端最终发出 `tts stop` 后才回到 `idle`，因此 UI 会长时间停在 `speaking`。

原因定位：

- 后端 `send_tts_message(state="stop")` 在真正发送 stop JSON 前，会先调用 `_wait_for_audio_completion()`。
- `_wait_for_audio_completion()` 原先直接等待 `AudioRateController.queue_empty_event.wait()`，没有单轮 35 秒 deadline。
- 长回复会产生大量 TTS binary frame，`AudioRateController` 按 60ms 帧时长慢慢发送队列；如果队列很长，`tts stop` 就必须等全部历史帧 drain 后才发。
- 根因在后端 TTS 收尾和单轮 timeout 策略缺失，不应通过放宽 ESP32 状态机解决。

解决方法：

- 新增后端单轮计时字段和方法：`round_timeout_seconds`、`start_round_timer()`、`round_time_remaining()`、`finish_round_timeout()`。
- ASR 进入 `recognizing` 前启动单轮计时；文本/BOOT 聊天入口在未启动时兜底启动。
- `send_tts_message(state="stop")` 等待音频队列时受单轮 deadline 约束。
- deadline 到期后，后端重置 `AudioRateController`、丢弃剩余待发音频、发送 `tts stop`，再发送 `ROUND_TIMEOUT` 错误和 `state=idle`。
- 新增配置项 `round_timeout: 35`。
- 新增回归测试，覆盖“单轮超时后 stop 不再无限等待队列 drain”。

验证结果：

```text
python3 -m unittest discover -s tests
PYTHONPYCACHEPREFIX=/private/tmp/xcb_pycache python3 -m py_compile core/handle/sendAudioHandle.py core/connection.py core/providers/asr/base.py core/handle/receiveAudioHandle.py tests/test_tts_start_order.py
```

后续注意：

- TTS 音频仍必须走 WebSocket binary frame，禁止恢复 base64 JSON TTS。
- 单个 binary frame 最大仍不得超过 2048 bytes。
- 长回复场景下，后端允许按 deadline 丢弃剩余未发送音频，以满足第一版“单轮对话总耗时超过 35 秒强制结束并回到 idle”的规则。
- 当前代码、容器和设备重连已验证；BOOT 长回复实机复测仍需继续触发确认。
- 后续实机复测应重点观察 `ROUND_TIMEOUT`、`tts stop received`、`Application: XiaoClaw tts_end -> idle` 的顺序和耗时。

## 13. TTS start 后又收到 thinking 导致 ESP32 状态机拒绝倒退

问题现象：

- BOOT 主链路已跑通，设备进入 `synthesizing` / `speaking` 并能收到 TTS binary frame。
- 串口同时出现：

```text
XiaoClawWS: tts start received
Application: XiaoClaw tts_start -> synthesizing
StateMachine: State: recognizing -> synthesizing
XiaoClawWS: server state=thinking
Application: XiaoClaw server state=thinking
StateMachine: Invalid state transition: synthesizing -> thinking
```

原因定位：

- 后端 `send_stt_message()` 在 STT 返回后立即发送 `type=tts,state=start`，但此时 LLM 尚未真正开始输出 TTS 音频。
- 后端 `ConnectionHandler.chat()` 随后在线程中异步发送 `state=thinking`，因此 ESP32 可能先收到 TTS start 并切到 `synthesizing`，再收到迟到的 `thinking`。
- 该问题属于后端控制消息时序错误，不是 WiFi、WebSocket runtime 配置或 ESP32 WakeNet/model 问题。

解决方法：

- `send_stt_message()` 只发送 STT 显示消息，不再提前发送 TTS start，也不提前标记 `client_is_speaking`。
- `sendAudioMessage()` 在第一段 TTS 音频即将发送时发送 `type=tts,state=start`，并在 `sentence_start` / binary audio 前完成。
- `ConnectionHandler.chat()` 对 `state=thinking` 的发送做短等待，避免 fast path 下 LLM/TTS 输出抢在 `thinking` 控制消息之前。
- 新增 `tests/test_tts_start_order.py` 固化后端控制消息顺序。

验证结果：

- 后端回归测试通过：

```text
python3 -m unittest tests.test_tts_start_order
```

- 后端语法检查通过：

```text
PYTHONPYCACHEPREFIX=/private/tmp/xcb_pycache python3 -m py_compile core/handle/sendAudioHandle.py core/connection.py tests/test_tts_start_order.py
```

- 重建并重启后端容器后，实机 BOOT 复测确认状态顺序变为：

```text
recognizing -> thinking -> synthesizing -> speaking -> idle
```

- 串口不再出现：

```text
Invalid state transition: synthesizing -> thinking
```

后续注意：

- TTS 音频仍必须走 WebSocket binary frame 小包下发，禁止恢复 base64 JSON 音频。
- 后端发送顺序应保持 `state=thinking` 早于 TTS start；TTS start 应贴近真实音频发送，而不是贴近 STT 返回。
- 如果未来 ESP32 仍遇到跨线程迟到状态消息，可再在 ESP32 端增加“忽略过期/倒退状态”的兼容层，但本次先修正后端源头顺序。

## 12. WS 连通后本地唤醒模型初始化失败

问题现象：

- XiaoClaw WS 已成功连通并收到服务端 hello 后，串口出现：

```text
MODEL_LOADER: Can not find model in partition table
AfeWakeWord: Failed to initialize wakenet model
AudioService: Failed to initialize wake word
```

- 该问题发生在 `ws connected` / `server hello received` 之后，不是 WiFi 或 XiaoClaw WS `reconnecting` 的根因。

原因定位：

- OpenClaw V2.7 使用 16MB 分区表，当前选择 `FLASH_NONE_ASSETS`，没有烧录 ESP-SR WakeNet/model 资源。
- ESP32-S3 + PSRAM 的 Kconfig 默认会选择 `USE_AFE_WAKE_WORD`，应用进入 `idle` 后调用 `EnableWakeWordDetection(true)`，从而懒加载 `AfeWakeWord` 并查找 `model` 分区。
- BOOT 录音链路还会启动 `AfeAudioProcessor`。该处理器原先也会主动调用 `esp_srmodel_init("model")` 查找可选 NS/VAD 模型，导致 BOOT 开始录音时残留一条 `MODEL_LOADER` 日志。

解决方法：

- OpenClaw V2.7 阶段一至阶段八明确禁用本地 wake word 初始化：
  - `CONFIG_WAKE_WORD_DISABLED=y`
  - `# CONFIG_USE_AFE_WAKE_WORD is not set`
  - `# CONFIG_SEND_WAKE_WORD_DATA is not set`
- `AudioService::EnableWakeWordDetection(true)` 在 `CONFIG_WAKE_WORD_DISABLED` 下直接 no-op，不再创建 `AfeWakeWord`。
- `scripts/release.py` 增加 Wake Word choice 清理，避免 release 构建时 `idf.py set-target` 生成的默认 `USE_AFE_WAKE_WORD` 残留。
- `AfeAudioProcessor` 不再在没有外部 `models_list` 时主动探测 `model` 分区；BOOT 录音继续走无模型 AFE / WebRTC VAD。
- 新增 `tests/test_openclaw_wake_word_policy.sh` 固化该策略。

验证结果：

- 策略测试通过：

```text
sh tests/test_openclaw_wake_word_policy.sh
```

- 固件构建通过：`idf.py build` 输出 `Project build complete.`
- 固件烧录成功：`idf.py -p /dev/cu.usbmodem5B5E1028821 flash` 输出 `Done`
- 实机启动确认：
  - WiFi 连接成功，IP 为 `192.168.31.128`
  - XiaoClaw WS 继续使用 runtime 地址连通
  - `ws connected` / `server hello received` 后不再出现 `MODEL_LOADER`、`AfeWakeWord`、`Failed to initialize wake word`
- 实机 BOOT 录音确认：
  - `DIAG_BOOT_PRESS`
  - `idle -> wakeup_detected -> listening -> uploading_audio`
  - 首个 Opus frame 上传成功，约 50 帧 Opus 上传成功
  - 后端返回 STT、TTS start 和 TTS binary frame，设备进入 `speaking`
  - BOOT 录音期间不再出现 `MODEL_LOADER`

后续注意：

- 阶段一至阶段八可以使用 BOOT 录音入口，但不能把这视为产品级本地语音唤醒完成。
- 阶段九仍必须评估并接入 ESP32 本地 WakeNet / ESP-SR / 自定义轻量唤醒词，不能迁移到后端持续 ASR。
- 如果未来重新启用 WakeNet，必须同步补齐模型资源分区或 assets 烧录策略，并重新验证 `idle -> wakeup_detected -> listening`。

## 11. 设备已连上 WiFi 但屏幕一直显示 reconnecting

问题现象：

- 屏幕右上角有 WiFi 标志，说明 STA 已联网。
- 屏幕主体持续显示 `reconnecting`。
- 串口显示设备 IP 为 `192.168.31.128`，但 XiaoClaw WS 仍连接旧地址：

```text
XiaoClawWS: ws config url=ws://192.168.1.10:8000/xiaozhi/v1/ ... url_source=build
EspTcp: Failed to connect to 192.168.1.10:8000, code=0x71
```

原因定位：

- `reconnecting` 是 XiaoClaw WebSocket 连接状态，不是 WiFi 状态。
- OpenClaw V2.7 当前启用 `CONFIG_XIAOZHI_SKIP_OTA_VERSION_CHECK=y`，因此 OTA version check 被跳过，`Ota::CheckVersion()` 不会执行，也不会从 OTA 响应写入 `Settings("websocket")`。
- 上一轮虽已让 `XiaoClawWsClient` 优先读取运行时配置，但 NVS 中实际没有新的 `websocket.url`，所以继续回退到编译期默认地址。
- OpenClaw 本地配置页原本只在 AP 配网模式启动；设备连上 WiFi 后用户无法直接在 STA 局域网里补写 XiaoClaw WS 地址。

解决方法：

- 在 `main/boards/common/wifi_board.cc` 的 OpenClaw 本地配置页中新增 XiaoClaw WebSocket 配置入口：
  - `ws_url`
  - `ws_token`
  - `ws_device_id`
  - `ws_client_id`
- 新增 `OpenClawRuntimeConfig::SaveXiaoClawWsConfig()`，将表单值保存到 `Settings("websocket")`。
- STA 联网成功后也启动 OpenClaw `:8080` 配置页，支持通过 `http://<device-ip>:8080/` 写入运行时配置。
- XiaoClaw WS 启动日志新增 `url_source/token_source/device_id_source/client_id_source`，明确区分运行时配置、编译期回退或空值。

验证结果：

- 重新构建成功：`idf.py build` 输出 `Project build complete.`
- 重新烧录成功：`idf.py -p /dev/cu.usbmodem5B5E1028821 flash` 输出 `Done`
- 串口确认 STA 联网后启动配置页：

```text
WifiStation: Got IP: 192.168.31.128
WifiBoard: OpenClaw API config server started on port 8080
```

- 通过本地配置页写入 `ws://192.168.31.53:8000/xiaozhi/v1/` 后，下一次重连成功切到运行时配置并连上后端：

```text
XiaoClawWS: ws config url=ws://192.168.31.53:8000/xiaozhi/v1/ ... url_source=runtime
XiaoClawWS: ws connected
Application: XiaoClaw WS connected
StateMachine: State: reconnecting -> idle
XiaoClawWS: server hello received
```

后续注意：

- 看到屏幕 `reconnecting` 且 WiFi 图标存在时，优先查 XiaoClaw WS 日志，不要再回到 WiFi 保存逻辑。
- 当前 OpenClaw V2.7 跳过 OTA version check，不能依赖 OTA 自动下发 `websocket` 配置；可先通过 `http://<device-ip>:8080/` 写入运行时地址。
- 后续若恢复 OTA 下发，需要重新评估 `CONFIG_XIAOZHI_SKIP_OTA_VERSION_CHECK` 与 XiaoClaw 本地配置页的职责边界。
- 本轮 WS 连通后出现 `AfeWakeWord: Failed to initialize wakenet model`，属于本地唤醒模型资源分区问题，不是 WS `reconnecting` 根因。

## 10. XiaoClaw WS 固件只读取编译期地址，换网络环境后继续连接旧 IP

问题现象：

- 设备完成配网并退出 `wifi_configuring` 后，开始进入 XiaoClaw WebSocket 连接阶段。
- 串口持续显示：

```text
XiaoClawWS: ws config url=ws://192.168.1.10:8000/xiaozhi/v1/
EspTcp: Failed to connect to 192.168.1.10:8000, code=0x71
WebSocket: Failed to connect to server
```

- 实际部署地点和局域网会变化，后端主机 IP 不是固定的 `192.168.1.10`，因此设备会在新环境里持续连接旧地址。

原因定位：

- `main/xiaoclaw_ws_client.cc` 启动时直接读取编译期 `CONFIG_XIAOCLAW_WS_URL`、`CONFIG_XIAOCLAW_DEVICE_TOKEN`、`CONFIG_XIAOCLAW_DEVICE_ID`、`CONFIG_XIAOCLAW_CLIENT_ID`。
- 虽然 OTA 流程已经会把后端返回的 `websocket.url` / `websocket.token` 写入 `Settings("websocket")`，但 XiaoClaw WS 客户端没有读取这份运行时配置。
- 因此设备每次联网后仍优先使用烧录时写进 `sdkconfig` 的旧地址，而不是运行时最新下发的后端地址。

解决方法：

- 扩展 `main/providers/openclaw_runtime_config_logic.[hc]`，增加逐字段解析 XiaoClaw WS 配置的纯逻辑 helper。
- 扩展 `main/providers/openclaw_runtime_config.[h|cc]`，新增 `GetXiaoClawWsConfig()`：
  - 优先读取 `Settings("websocket")` 下的 `url/token/device_id/client_id`
  - 缺失时回退到编译期 `CONFIG_XIAOCLAW_*`
- 修改 `main/xiaoclaw_ws_client.cc`：
  - `Start()` 改为使用 `OpenClawRuntimeConfig::GetXiaoClawWsConfig()`
  - `SendHelloJson()` 改为复用同一份解析出的 `device_id/client_id`
- 新增/扩展主机侧最小测试，验证“运行时字段优先、缺失时回退到编译期默认值”的行为。

验证结果：

- 主机侧最小测试通过：

```text
cc -I main/providers tests/test_openclaw_runtime_config_logic.c \
   main/providers/openclaw_runtime_config_logic.c \
   -o /tmp/test_openclaw_runtime_config_logic && /tmp/test_openclaw_runtime_config_logic
```

- 测试覆盖确认：
  - runtime `url` 可覆盖 build `url`
  - runtime 空 `token` 时回退到 build `token`
  - runtime 缺失 `device_id` 时回退到 build `device_id`
  - runtime `client_id` 可覆盖 build `client_id`
- 当前会话环境缺少可直接调用的 `idf.py`，因此尚未在本轮完成完整固件重编译与实机串口复测。

后续注意：

- XiaoClaw WS 的服务地址不能依赖 `sdkconfig` 写死 IP；编译期配置只能作为兜底默认值。
- 以后若设备换网络环境后仍尝试连接旧地址，第一步先查串口 `XiaoClawWS: ws config url=...`，确认是否已经读取到 OTA/NVS 下发的运行时地址。
- 若运行时地址已更新但仍连不上，再继续区分是 TCP 不可达、端口未监听，还是后端服务未真正启动。

## 8. 固件烧录后 TFT 白屏、BOOT / 音量键无反应

问题现象：

- 烧录固件后，OpenClaw V2.7 设备 TFT 一片白，没有任何 UI 显示。
- BOOT、VOL+、VOL- 按键没有预期反应。

原因定位：

- 当前 `sdkconfig` 实际选中的是 `CONFIG_BOARD_TYPE_BREAD_COMPACT_WIFI=y`，不是 `CONFIG_BOARD_TYPE_OPENCLAW_V2_7=y`。
- `bread-compact-wifi` 板级文件初始化的是 I2C OLED，使用 `DISPLAY_SDA=GPIO41`、`DISPLAY_SCL=GPIO42`；OpenClaw V2.7 实物是 SPI ST7735，使用 `SCK=GPIO21`、`MOSI=GPIO47`、`DC=GPIO40`、`CS=GPIO41`、`BL=GPIO42`。
- `bread-compact-wifi` 还把 `VOL+` 配到 `GPIO40`，与 OpenClaw 的 `TFT_DC=GPIO40` 冲突；OpenClaw 正确 `VOL+` 是飞线 `GPIO38`。
- `scripts/release.py openclaw-v2.7` 原先只把 `CONFIG_BOARD_TYPE_OPENCLAW_V2_7=y` 追加到 `sdkconfig` 末尾，没有清掉 `idf.py set-target` 生成的默认 `CONFIG_BOARD_TYPE_BREAD_COMPACT_WIFI=y`。由于 `main/CMakeLists.txt` 先判断 `BREAD_COMPACT_WIFI`，最终仍可能编出错误板型固件。

解决方法：

- 修改 `scripts/release.py`：在追加目标板型配置前，先清理已有 `CONFIG_BOARD_TYPE_*` choice，避免多个板型同时为 `y`。
- 同步清理 `Flash Assets` choice，避免 `CONFIG_FLASH_NONE_ASSETS=y` 与默认 `CONFIG_FLASH_DEFAULT_ASSETS=y` 残留冲突。
- 将当前本地 `sdkconfig` 收口为：
  - `CONFIG_BOARD_TYPE_OPENCLAW_V2_7=y`
  - `CONFIG_FLASH_NONE_ASSETS=y`
  - `CONFIG_PARTITION_TABLE_FILENAME="partitions/openclaw_v2_7_16m.csv"`
  - `CONFIG_ESPTOOLPY_FLASHMODE="dio"`
  - `CONFIG_ESPTOOLPY_FLASHSIZE="16MB"`

验证结果：

- `idf.py build` 通过，输出 `Project build complete.`
- 构建过程确认编译 `main/boards/openclaw-v2.7/openclaw_v2_7_board.cc.obj`。
- CMake 输出确认 `Assets flashing disabled (FLASH_NONE_ASSETS)`。
- 最终烧录命令不再包含 `generated_assets.bin`，只包含 bootloader、partition table、ota data 和 `xiaozhi.bin`。
- 2026-05-26 实机重新烧录后，串口启动日志确认 `SKU=openclaw-v2.7`、`LcdDisplay` 初始化、背光打开、`OpenClaw V2.7 board initialized`。
- 2026-05-26 实机串口日志确认 `BOOT` 出现 `DIAG_BOOT_PRESS`，`VOL+ / VOL-` 触发音量变化日志，监控期间无 panic、无异常重启。

后续注意：

- 以后排查白屏/按键无响应，第一步检查 `sdkconfig` 的 `CONFIG_BOARD_TYPE_*`，不能只看 `sdkconfig.defaults.esp32s3` 或 `config.json`。
- 若使用 release 脚本打包，必须确认 `build/compile_commands.json` 中 `BOARD_TYPE="openclaw-v2.7"`。
- OpenClaw V2.7 的 `GPIO40` 固定给 TFT_DC，不能恢复为 VOL+。

## 9. 配网页面保存 WiFi 后设备仍停留在配网模式

问题现象：

- 设备进入配网模式后，手机连接 `Xiaozhi-1279`，在页面填写 WiFi 信息保存。
- 页面侧看起来提交成功，但设备没有退出配网模式，用户体感像“保存了也不生效”。

原因定位：

- 默认配网页面 `http://192.168.4.1` 实际走的是 `managed_components/78__esp-wifi-connect/wifi_configuration_ap.cc` 的 `/submit`。
- 原实现中，`/submit` 在 `ConnectToWifi()` 和 `Save()` 成功后只返回 `{"success":true}`，**没有触发退出配网模式**。
- OpenClaw 自己的 `http://192.168.4.1:8080` 页面另有一套保存逻辑，原先还会在 HTTP 请求线程里直接 `StopStation()/StopConfigAp()/StartStation()`，切换路径也不够稳。
- 因此用户提交成功后，凭据虽然可能已经保存，但当前这轮 AP 模式不会自动收口，设备仍留在配网态。

解决方法：

- 修改 `main/boards/common/wifi_board.cc`：
  - 保存成功后先返回 HTTP 响应。
  - 再通过 `Application::Schedule()` 在主任务里调用 `WifiManager::GetInstance().StopConfigAp()`。
  - 统一交给既有 `ConfigModeExit -> TryWifiConnect()` 链路切入 STA 模式。
- 修改 `managed_components/78__esp-wifi-connect/wifi_configuration_ap.cc`：
  - 默认配网页面 `/submit` 成功返回后，延迟 200ms 调用 `on_exit_requested_()`。
  - 保证前端拿到成功响应后，设备自动退出配网模式。
- 使用 `erase-flash -> flash -> monitor` 做干净实机复测，排除旧 NVS 残留。

验证结果：

- 擦除整片 flash 后，设备启动日志确认 `NVS namespace wifi doesn't exist`，并进入 `wifi_configuring`。
- 串口确认热点 `Xiaozhi-1279`、`http://192.168.4.1`、`http://192.168.4.1:8080` 均正常启动。
- 用户重新完成配网后，设备不再停留在 `wifi_configuring`，串口已进入 XiaoClaw WebSocket 连接重试阶段，说明配网退出和联网切换链路已打通。
- 当前剩余报错已切换为 `ws://192.168.1.10:8000/xiaozhi/v1/` 不可达，不属于配网页面保存逻辑本身。

后续注意：

- 后续若用户反馈“配网页面保存不生效”，先区分访问的是 `192.168.4.1` 默认页还是 `192.168.4.1:8080` OpenClaw 配置页。
- 两个页面现在都应在保存成功后自动退出配网模式；如果仍异常，先查串口是否出现 `ConfigModeExit`、`StartStation` 和 `Got IP`。
- 若设备退出配网后仍不可用，要优先看后端连通性、token 和 WS 地址，而不是重复怀疑 WiFi 保存逻辑。

## 7. oMLX 控制台未加载 ASR 模型，后端 ASR 请求返回 404

问题现象：

- BOOT 录音后，后端已出现 `[LISTEN] START`、`[WS_BINARY] FIRST frame`、`[ASR_DIAG] manual stop captured frames` 和 `ASR triggering manually`。
- 后端 ASR provider 记录 `API请求失败: 404`，随后按当前逻辑返回 `ASR_EMPTY`。
- oMLX 控制台未看到加载 ASR 模型记录。

原因定位：

- 后端已经走到 ASR provider，并非未触发 ASR。
- 容器内 `http://host.docker.internal:8009/v1/models` 可访问，模型列表包含 `Qwen3-ASR-1.7B-8bit`。
- 容器内 `GET /v1/audio/transcriptions` 返回 `405 Method Not Allowed`，说明 ASR 路由存在，需使用 POST。
- 容器实际加载配置中 `OMLXASR.model_name` 仍是占位值 `omlx-asr`，与 oMLX 实际模型名不一致，因此请求未命中可用 ASR 模型。

解决方法：

- 将 `config.yaml` 与本地覆盖配置 `data/.config.yaml` 的 `ASR.OMLXASR.model_name` 改为 `Qwen3-ASR-1.7B-8bit`。
- 保持容器内访问地址为 `http://host.docker.internal:8009/v1/audio/transcriptions`；宿主机控制台显示的 `http://127.0.0.1:8009/v1` 不能直接作为容器内回环地址使用。
- 重启 `xiaozhi-esp32-server` 容器，让配置重新加载。

验证结果：

- 容器重启后为 `healthy`。
- 容器内加载配置确认：`selected ASR OMLXASR`，`OMLXASR base_url http://host.docker.internal:8009/v1/audio/transcriptions`，`OMLXASR model_name Qwen3-ASR-1.7B-8bit`。
- ESP32 已自动重连并继续发送 ping。
- 2026-05-25 BOOT 复测已通过：主链路成功走通 `[LISTEN] START -> WS_BINARY -> [LISTEN] STOP -> ASR -> LLM -> TTS`。
- 本次 ASR 成功返回识别文本 `你好，你是谁？`，说明 404 阻塞已消除，不再落入假 `ASR_EMPTY`。
- 后端随后成功完成 LLM 与三段 TTS 音频下发，P5/P7 第一版端到端主链路已闭环。

后续注意：

- 排查 oMLX ASR 时先确认 `data/.config.yaml`，它会覆盖默认配置。
- 如果宿主机给出的 oMLX 地址是 `127.0.0.1:8009`，Docker 容器内应使用 `host.docker.internal:8009`。
- `OMLX_ASR_MODEL` 环境变量当前未注入容器；如果未来改为环境变量配置，需要同步 compose 的 environment/env_file。

## 6. 后端 Docker 重启后仍加载 FunASR 并因 model.pt 空目录失败

问题现象：

- Docker 启动方式改为 `python app.py` 后，后端容器反复重启。
- 日志显示 `IsADirectoryError: [Errno 21] Is a directory: 'models/SenseVoiceSmall/model.pt'`。
- 即使默认 `config.yaml` 改成 oMLX provider，容器仍一度继续加载 `FunASR/ChatGLMLLM/EdgeTTS`。

原因定位：

- `docker-compose_all.yml` 原先把宿主机 `./models/SenseVoiceSmall/model.pt` 挂载到容器同一路径；当宿主机文件不存在时，Docker 会创建同名空目录，导致 FunASR 把目录当模型文件读取。
- 实际运行优先读取 `main/xiaozhi-server/data/.config.yaml`，该本地配置仍选择 `FunASR/ChatGLMLLM/EdgeTTS`，覆盖了默认配置。
- 本项目当前后端应连接本地 oMLX 模型服务，ASR/TTS/LLM 都不应走 FunASR 本地模型路径。

解决方法：

- 移除 `docker-compose.yml` 和 `docker-compose_all.yml` 中的 `model.pt` 文件挂载。
- 删除 Docker 自动创建的空 `models/SenseVoiceSmall/model.pt` 目录。
- 将默认配置与本地 `data/.config.yaml` 的 selected_module 改为 `OMLXASR/OMLXLLM/OMLXTTS`。
- 新增 oMLX provider 配置，默认指向 `host.docker.internal:8009`。
- 重建并重启后端容器，使旧挂载和旧配置失效。

验证结果：

- `python3 -m py_compile` 关键后端文件通过。
- `docker compose ps` 显示 `xiaozhi-esp32-server` 为 `Up ... (healthy)`。
- 后端日志显示 `初始化组件: llm成功 OMLXLLM`、`初始化组件: asr成功 OMLXASR`，不再进入 FunASR 初始化错误。

后续注意：

- `data/.config.yaml` 是本地 ignored 覆盖配置，排查 provider 选择时必须同时检查它。
- 不要恢复 `model.pt` 文件挂载；如未来确实需要本地 FunASR，应先放置真实模型文件，再单独评估。
- `.env.example` 只能保留空值模板；真实 oMLX token 或模型名放 `.env` / 本地配置，不写入源码或日志。

## 4. XiaoClaw WS 启动失败后可能卡在 reconnecting

问题现象：

- 在补充 XiaoClaw WS 心跳保活与自动重连后，代码验收发现：如果 `XiaoClawWsClient::Start()` 在初次连接或客户端重建时失败，固件会进入 `reconnecting` 并 `reset()` 客户端。
- 该失败路径没有继续安排下一次重连，可能导致后端临时不可用、网络刚恢复但服务未就绪等场景下设备长期停留在 `reconnecting`。

原因定位：

- `InitializeXiaoClawWsClient()` 的 `Start()` 失败分支只更新显示状态和设备状态，然后释放 `xiaoclaw_ws_client_`。
- `TryXiaoClawReconnect()` 在客户端为空时会调用 `InitializeXiaoClawWsClient()` 后直接返回；如果这次 `Start()` 仍失败，就不会进入后续退避重连分支。

解决方法：

- 在 `InitializeXiaoClawWsClient()` 的 `Start()` 失败分支中增加退避和定时器调度：
  - `xiaoclaw_reconnect_delay_ms_ = std::min(xiaoclaw_reconnect_delay_ms_ * 2, 30000);`
  - `StartXiaoClawReconnectTimer();`

验证结果：

- `idf.py build` 通过，输出 `Project build complete.`
- 后续仍需上板验证：空闲 5 分钟不断线、后端重启后自动恢复、恢复后 BOOT 可重新触发录音。

后续注意：

- 24 小时在线设备的连接层必须允许服务端清理死连接，但客户端必须具备持续心跳和失败后反复重连能力。
- 以后修改重连逻辑时，要同时覆盖三类路径：正常断线、重连失败、初次启动失败。

## 5. 后端收到 ping 但仍按 180 秒关闭连接

问题现象：

- ESP32 每 30 秒发送 `{"type":"ping"}`，后端日志也连续记录“收到ping消息”。
- 但连接仍在约 180 秒后出现“连接超时，准备关闭”，ESP32 随后进入 `reconnecting`。

原因定位：

- 单独依赖 ping handler 刷新 `last_activity_time` 不够稳妥；实际验收中超时线程没有看到活动时间被刷新。
- 对 24 小时在线设备而言，任何通过协议校验的入站帧都应视为连接活动，而不应只依赖某一个消息处理器。

解决方法：

- 在 `ConnectionHandler._route_message()` 中，通过 `validate_inbound_frame()` 后立即执行：
  - `self.last_activity_time = time.time() * 1000`
- 这样 `ping`、`hello`、`listen`、binary 音频帧都会刷新连接活动时间。

验证结果：

- 上板后后端从 06:19:59 到 06:31:59 持续收到 ping，未再出现“连接超时，准备关闭”。
- ESP32 串口超过原 180 秒断开窗口后持续保持在线。

后续注意：

- 当前 Docker 容器主进程是 `tail -f /dev/null`，后端 Python 服务需要在容器内单独启动；本次验收通过 `docker exec -d ... python app.py` 恢复服务。
- 后续部署整理时应把 `python app.py` 固化为容器主进程或 compose command，避免 `docker restart` 后服务未自动恢复。

## 1. INMP441 麦克风无有效音频

问题现象：

- I2S 麦克风链路初期没有有效音频输入。
- 采样数据异常，无法得到稳定的人声/环境声峰值。

原因定位：

- 后续检查发现 INMP441 模块 `SD` 与 `VDD` 存在短接。
- 该问题不是 I2S 引脚配置本身导致，而是麦克风模块硬件连接异常。

解决方法：

- 拆开短接点，并重新处理/更换麦克风模块。
- 固件继续使用最终引脚：`WS=GPIO4`、`SCK=GPIO5`、`SD=GPIO6`。
- INMP441 的 `L/R` 接 `GND`，按左声道输出处理。

验证结果：

```text
WS(GPIO4) 有时钟跳变
SCK(GPIO5) 有时钟跳变
SD(GPIO6) 有数据跳变
原始 I2S 非全 0 / 非全 1
左声道峰值约 57000
右声道为 0
```

后续注意：

- INMP441 若 `L/R=GND`，只应期待左声道有效。
- 后续 ASR 接入前，仍要保留麦克风峰值页作为硬件快速自检入口。

## 2. TFT_DC 与 VOL+ 复用冲突

问题现象：

- 8 针 TFT 使用 `GPIO40` 作为 `DC` 后，`VOL+` 按键无法正常检测。
- 拔掉 TFT 跳帽后，`GPIO40` 在 15 秒采样窗口内始终高电平，`transitions=0`。
- `VOL- / GPIO39`、`BOOT / GPIO0`、`RESET / RST` 均正常，只有 `VOL+` 异常。

原因定位：

- 小智 AI 扩展板 44P V1.1 原始设计中，`VOL+` 与 `TFT_DC` 都涉及 `GPIO40`。
- 接 8 针 TFT 时，`GPIO40` 必须保留给 TFT `DC`。
- 当前跳帽/复用方式下，`VOL+` 支路无法同时可靠接入 `GPIO40`。

解决方法：

- `TFT_DC` 固定继续使用 `GPIO40`。
- `VOL+` 按键非 GND 脚飞线改接到 `GPIO38`。
- 正式固件中固定：

```c
#define BTN_VOL_UP_PIN   38
#define BTN_VOL_DN_PIN   39
#define BTN_BOOT_PIN      0
#define TFT_DC_PIN       40
```

验证结果：

```text
GPIO38 空闲电平=1
GPIO38 变化: 1 -> 0
GPIO38 变化: 0 -> 1
汇总: transitions=36
[PASS] GPIO38 能检测到 VOL+ 电平变化。
```

后续注意：

- 方案、固件、文档都不能再把 `VOL+` 写成 `GPIO40`。
- `GPIO40` 是 TFT 专用 `DC`，不要再复用给按键。

## 3. MUTE 键误解为独立按键

问题现象：

- 早期设计中把 `GPIO0` 写成 `MUTE`，准备做静音/取消静音。
- 实物板上实际只有 `VOL+`、`VOL-`、`RESET`、`BOOT` 四个按键，没有独立 MUTE 键。

原因定位：

- 原理图/标注中 `MUTE/BOOT` 容易让人误以为存在独立静音键。
- 实物按键确认后，`GPIO0` 对应的是 BOOT 键。
- BOOT 在正常运行中可以作为功能键使用，但要避免影响下载/启动流程。

解决方法：

- 移除静音/取消静音逻辑。
- 将 `BOOT(GPIO0)` 定义为功能键：
  - 主界面短按 BOOT：进入设置菜单。
  - 菜单内 `VOL+ / VOL-`：上下选择。
  - 菜单内短按 BOOT：选择当前项目。
  - 详情页短按 BOOT：返回菜单。

验证结果：

- 当前固件菜单可通过 BOOT 正常进入和选择。
- 已实现菜单项包括：`NETWORK`、`NET TEST`、`TIME SYNC`、`AUDIO TEST`、`MIC TEST`、`SYSTEM INFO`、`EXIT`。

后续注意：

- 后续若要做“长按恢复出厂网络设置”，可以继续复用 BOOT 长按，但短按行为不要改掉。
- 文案中统一写 `BOOT 功能键`，不要再写 MUTE。

## 4. 音量键不能只当硬件音量

问题现象：

- 早期可能误以为音量键是硬件层直接控制功放响度。
- 实际项目需要让音量设置影响 I2S/TTS 播放，并且重启后保持。

原因定位：

- `VOL+ / VOL-` 是普通 GPIO 按键。
- MAX98357A 功放没有通过这两个按键直接改变数字音频音量。
- 因此音量应由固件读取按键后做软件增益控制。

解决方法：

- 新增软件音量状态，范围 `0..100`，步进 `10`。
- 播放提示音时按当前音量缩放 sample。
- 音量写入 NVS key `volume`，启动时从 NVS 恢复。
- 增加主机侧逻辑测试覆盖音量限制和缩放。

验证结果：

- `VOL+` 提高软件音量并播放较高提示音。
- `VOL-` 降低软件音量并播放较低提示音。
- 重启后音量从 NVS 恢复。
- 功放提示音响度受当前软件音量影响。

后续注意：

- 后续 TTS 播放也必须走同一套软件音量缩放。
- 需要避免音量为 0 时误判为功放故障。

## 5. 手机访问配网页面出现 HTTP 431

问题现象：

- AP 配网启动后，手机连接 `OpenClaw-Setup` 并访问 `http://192.168.4.1`。
- 部分手机浏览器请求头较大，ESP HTTP server 返回 `431 Request Header Fields Too Large`。

原因定位：

- ESP-IDF 默认 HTTP server 请求头长度偏小。
- 手机浏览器可能携带较长的 Header，超过默认限制。

解决方法：

- 在 `hardware_bringup/sdkconfig.defaults` 和 `hardware_bringup/sdkconfig` 中增加/同步：

```text
CONFIG_HTTPD_MAX_REQ_HDR_LEN=2048
CONFIG_HTTPD_MAX_URI_LEN=1024
```

验证结果：

- 手机可正常打开配网页面。
- 表单可提交 SSID 和密码。
- 固件保存 Wi-Fi 凭据到 NVS，并启动 STA 连接。

后续注意：

- 后续配网页面如果增加字段，也要关注 URI/header/body 长度。
- 表单 body 当前仍应保持小而简单，避免把配网页面做成复杂 Web App。

## 6. Wi-Fi 需要支持后续重新配网

问题现象：

- 用户明确关心：后续更换网络环境时能不能重新配网。
- 早期如果只在首次烧录时写死 Wi-Fi，会导致搬家、换路由器、改密码后不可维护。

原因定位：

- 老人设备必须支持运行时重新配置网络。
- Wi-Fi 信息不能只作为编译期参数写死在固件中。

解决方法：

- 菜单中保留 `NETWORK` 项。
- 选择 `NETWORK` 后启动 AP：

```text
SSID: OpenClaw-Setup
URL:  http://192.168.4.1
```

- 手机提交 SSID/密码后，保存到 NVS：

```text
wifi_ssid
wifi_pass
```

- 设备重启后自动读取 NVS 并连接已保存网络。

验证结果：

```text
Setup AP started: SSID=OpenClaw-Setup URL=http://192.168.4.1
Wi-Fi connected: 192.168.31.128
SSID: MadBull
```

后续注意：

- 后续建议增加“清除 Wi-Fi 配置”或“长按 BOOT 进入强制配网”。
- 目前 `NETWORK` 菜单已经可以用于更换网络。

## 7. 外网连通性不能只看 Wi-Fi 已连接

问题现象：

- Wi-Fi 已连接只能说明设备接入了局域网，不能证明公网 HTTP/HTTPS 可达。
- 后续接云端 API 前，需要有一个独立的网络探测入口。

原因定位：

- 路由器、DNS、运营商网络、网关限制都可能导致“Wi-Fi 已连接但外网不可用”。

解决方法：

- 新增 `NET TEST` 菜单项。
- 请求 `http://example.com/`。
- 将 HTTP `200..399` 判定为通过。
- TFT 显示 `RUNNING / OK / FAIL` 和 HTTP 状态码。
- 串口输出详细结果。

验证结果：

```text
Network test OK: HTTP 200
```

后续注意：

- `NET TEST` 只证明 HTTP 公网可达，不等于 HTTPS/API 可用。
- 下一步还需要 `API TEST` 验证证书、时间、鉴权和云端接口。

## 8. SNTP 时间同步是 HTTPS/API 前置条件

问题现象：

- 后续 HTTPS、证书校验、日志时间戳、提醒任务都需要可靠系统时间。
- ESP32 启动初期系统时间可能停留在 1970 附近，不能直接认为时间有效。

原因定位：

- ESP32-S3 没有电池 RTC 时，重启后需要联网同步时间。
- 如果系统时间无效，HTTPS 证书有效期校验可能失败，提醒任务也无法可靠运行。

解决方法：

- 使用 ESP-IDF 5.3.2 的 `esp_netif_sntp.h` API：

```c
esp_sntp_config_t config = ESP_NETIF_SNTP_DEFAULT_CONFIG("pool.ntp.org");
esp_netif_sntp_init(&config);
esp_netif_sntp_sync_wait(pdMS_TO_TICKS(12000));
```

- Wi-Fi 获取 IP 后自动启动 SNTP。
- 菜单中新增 `TIME SYNC`，用于查看状态和手动重试。
- 新增 `time_sync_epoch_valid()`，将 `2024-01-01 00:00:00` 之后的 epoch 视为有效时间。

验证结果：

```text
Wi-Fi connected: 192.168.31.128
Time sync OK: 1778076020
local=2026-05-06 22:00:20 CST
```

后续注意：

- 进入 HTTPS/API 请求前，应检查 Wi-Fi 已连接且 SNTP 已同步。
- 如果后续离线提醒需要跨重启保存，仍要设计本地任务持久化和重启恢复策略。

## 9. SNTP 日期显示触发编译警告

问题现象：

- 增加 `TIME SYNC` 页面后，ESP-IDF 编译失败。
- 报错来自 `-Werror=format-truncation=`，认为日期 `snprintf()` 可能截断。

原因定位：

- ESP-IDF 默认把部分 warning 当 error。
- 虽然实际日期字符串很短，但编译器从 `struct tm` 的 int 范围推导，认为 `%04d-%02d-%02d` 理论上可能超过 `char line[24]`。

解决方法：

- 将 `draw_time_sync_dynamic()` 中的显示缓冲区从 `char line[24]` 增大为 `char line[40]`。

验证结果：

- `idf.py build` 重新通过。
- 固件成功刷写并完成 SNTP 实测。

后续注意：

- ESP-IDF 固件中所有 `snprintf()` 都要给足缓冲区。
- 不要依赖“实际值肯定很短”来压缩显示 buffer。

## 10. idf.py monitor 需要 TTY

问题现象：

- 运行 `idf.py monitor` 时出现：

```text
Error: Monitor requires standard input to be attached to TTY.
```

原因定位：

- ESP-IDF monitor 需要交互式 TTY。
- 普通非 TTY shell 调用无法满足 monitor 的输入要求。

解决方法：

- 启动 monitor 时使用带 TTY 的命令执行方式。
- 结束监视器时发送 `Ctrl+]`。

验证结果：

- 成功打开串口监视器。
- 成功观察启动、Wi-Fi 自动连接、`NET TEST`、`SNTP` 日志。

后续注意：

- 最终回复前必须退出 monitor，避免串口被占用。
- 刷写和 monitor 都需要访问 `/dev/cu.usbmodem5B5E1028821`。

## 11. 当前已确认的稳定底座

截至 2026-05-07，以下能力已上板验证：

```text
硬件平台：ESP32-S3-N16R8 + 小智AI扩展板44P V1.1
串口：/dev/cu.usbmodem5B5E1028821
最终引脚：V2.7
TFT：正常
按键：VOL+=GPIO38 / VOL-=GPIO39 / BOOT=GPIO0 / RESET=RST
麦克风：GPIO4/5/6，峰值采样正常
功放：GPIO7/15/16，提示音播放正常
音量：软件增益 + NVS 保存
配网：OpenClaw-Setup + http://192.168.4.1
自动联网：NVS Wi-Fi 凭据恢复
外网探测：NET TEST HTTP 200
时间同步：SNTP OK
```

下一步问题记录应优先覆盖：

1. HTTPS 证书链。
2. API Key / Base URL 配置方式。
3. `API TEST` 的错误分类和屏幕提示。
4. ASR/TTS 音频格式、延迟和失败降级。

## 12. API TEST 先打通 HTTPS 和鉴权层

问题现象：

- 项目准备进入真实云端 API，但如果直接做 ASR/TTS/LLM 语音闭环，失败时很难判断是网络、SNTP、证书、API Key、模型接口还是音频格式问题。

原因定位：

- `NET TEST` 只证明 HTTP 公网可达，不证明 HTTPS 证书校验和 API 鉴权可用。
- HTTPS 证书校验依赖有效系统时间，因此必须在请求前检查 SNTP。
- OpenAI-compatible 服务需要 Base URL、API Key、模型名等配置，后续会迁移到 NVS/配网页面，但当前阶段先用编译期默认值更容易验证链路。

解决方法：

- 菜单新增 `API TEST`，位置在 `TIME SYNC` 之后。
- 新增 API 配置结构：

```c
base_url   = OPENCLAW_API_BASE_URL
api_key    = OPENCLAW_API_KEY
test_model = OPENCLAW_API_TEST_MODEL
```

- 初始版本请求 `https://api.openai.com/v1/models`；当前版本已切换为最小文本 `POST /chat/completions` 探测。Base URL、Key 和模型名支持构建时通过环境变量覆盖：

```text
OPENCLAW_API_BASE_URL
OPENCLAW_API_KEY
OPENCLAW_API_TEST_MODEL
```

- API 配置同时接入 NVS 和配网页面：
  - `api_base` 保存 API Base URL。
  - `api_key` 保存 API Key，表单留空时保留当前 Key。
  - `api_model` 保存测试模型名。
- HTTPS 使用 `esp_crt_bundle_attach` 和 ESP-IDF 内置 CA 证书包。
- 请求前检查：
  - Wi-Fi 状态为 `CONNECTED`。
  - 当前 epoch 通过 `time_sync_epoch_valid()`。
- TFT 显示：
  - `RUNNING / OK / FAIL`
  - `HTTP xxx`
  - `ERR <类别>`
- 串口输出 Base URL、测试模型名、Key 是否存在、HTTP 状态、`esp_err_t`、socket errno 和错误分类。
- chat 探测会捕获部分响应体，并检查响应中是否包含 `chat.completion`、`choices` 和 `message`，避免 HTTP 200 但接口返回非 chat 结构时误判成功。
- 启动日志只输出 Key 的 `SET/EMPTY` 状态，不输出 Key 明文。

验证结果：

```text
主机侧逻辑测试：test_api_config_logic / test_api_test_logic / test_net_test_logic / test_time_sync_logic / test_mvp_logic / test_wifi_logic 均通过
ESP-IDF build：通过
固件产物：build/openclaw_voice_hardware_bringup.bin
刷写：通过，/dev/cu.usbmodem5B5E1028821
启动：PSRAM OK / MIC READY / SPK READY / KEYS READY
Wi-Fi：connected with MadBull, IP 192.168.31.128
SNTP：Time sync OK: 1778113345
API TEST：进入菜单后发起 https://api.openai.com/v1/models
API TEST 结果：class=TIMEOUT ret=ESP_ERR_HTTP_CONNECT errno=0 elapsed=10023ms HTTP=0
DeepSeek API TEST：进入菜单后发起 https://api.deepseek.com/chat/completions
DeepSeek API TEST 结果：Certificate validated, HTTP 200, object=chat.completion
API CONFIG：启动日志显示 base=https://api.deepseek.com model=deepseek-v4-flash key=SET
前置检查：SNTP 无效时 API TEST 显示 NO TIME；时间有效后同一配置返回 HTTP 200
```

本轮尚未完成：

```text
默认 OpenAI HTTP 2xx：未完成
原因：默认 Base URL https://api.openai.com/v1 在当前网络下连接阶段 10 秒超时；默认 API Key 为空
DeepSeek HTTP 2xx：已完成，GET https://api.deepseek.com/models 返回 HTTP 200
DeepSeek chat/completions：已完成，POST https://api.deepseek.com/chat/completions 返回 HTTP 200
```

后续注意：

- 默认 `OPENCLAW_API_KEY` 为空时，真实 OpenAI-compatible 服务应返回 401/403，并在 TFT/串口上显示 `AUTH`。
- DeepSeek 官方 API 已拿到 `/models` 和 `/chat/completions` HTTP 200，API 配置已迁移到 NVS/配网页面。
- 最小文本 `chat/completions` 已通过；仍不要直接把 ASR/TTS 和 LLM 语音闭环一次性接满，应分阶段接入录音上传、ASR、TTS 播放。
- 当前 OpenAI-compatible LLM 配置入口已经具备；下一步进入录音上传 / ASR / TTS 时仍要分段验证。
- 用户已提供 DeepSeek 官方开发阶段 API Key；不得写入文档和代码明文，设备真正使用前替换。
- DeepSeek 当前测试参数：
  - Base URL：`https://api.deepseek.com`
  - 首选模型：`deepseek-v4-flash`
  - 已完成探测：`GET https://api.deepseek.com/models` -> HTTP 200
  - 已完成探测：`POST https://api.deepseek.com/chat/completions` -> HTTP 200
  - 已完成配置：API Base URL / Key / 模型名写入 NVS/配网页面
- 下一阶段开始语音链路时，应先做录音缓冲和音频格式探测，再分别接入 ASR、LLM、TTS。

## 13. REC TEST 先验证录音缓冲

问题现象：

- 进入 ASR 上传前，需要先确认麦克风采样能形成稳定的、可上传的 PCM 缓冲。
- 如果直接接 ASR，失败时无法快速判断是麦克风采样、音频格式、HTTP 上传、鉴权还是 ASR 模型问题。

解决方法：

- 菜单新增 `REC TEST`，位置在 `API TEST` 后。
- 录音规格先固定为：

```text
采样率：16000 Hz
声道：mono
格式：PCM16
时长：2000 ms
目标样本数：32000
目标字节数：64000
```

- 录音任务运行时，常驻麦克风峰值任务临时让出 I2S 读取，避免两个任务同时读取同一个麦克风通道。
- 新增纯逻辑测试 `test_audio_capture_logic`，覆盖 PCM16 Peak 和 RMS 计算。
- TFT 显示 `REC TEST` 状态、Peak、RMS。
- 串口输出样本数、字节数、Peak、RMS 和录音时长。

验证结果：

```text
主机侧逻辑测试：test_audio_capture_logic 通过
ESP-IDF build：通过
刷写：通过，/dev/cu.usbmodem5B5E1028821
REC TEST：samples=32000 bytes=64000 peak=2975 rms=322 duration_ms=2000
```

结论：

- 麦克风到 2 秒 PCM16 音频缓冲的链路已验证。
- 下一步可以在此缓冲基础上做 WAV 封装或 multipart/body 上传，再接 ASR API。

## 14. SNTP 单服务器偶发超时

问题现象：

- 首次刷写 `API TEST` 后，设备成功连接 Wi-Fi，但 SNTP 在 12 秒等待窗口内超时：

```text
Time sync failed: ret=ESP_ERR_TIMEOUT epoch=15
API test skipped: NO TIME
```

原因定位：

- 之前只配置了 `pool.ntp.org` 一个 SNTP server。
- 单一 NTP 入口在当前网络下可能偶发解析、路由或响应超时。
- API TEST 正确拒绝在时间无效时发起 HTTPS，这是预期保护行为。

解决方法：

- 将 SNTP server 数量从 1 提高到 3：

```text
CONFIG_LWIP_SNTP_MAX_SERVERS=3
```

- 固件改为多服务器：

```text
pool.ntp.org
time.google.com
ntp.aliyun.com
```

验证结果：

```text
Time sync OK: 1778113345
```

后续注意：

- HTTPS/API 前置检查仍保留，不允许在时间无效时请求云端。
- 如果后续部署网络对海外 NTP 不稳定，可以优先保留国内可达 NTP。

## 15. 默认 OpenAI Base URL 在当前网络下连接超时

问题现象：

- 进入 `API TEST` 后，固件发起请求：

```text
https://api.openai.com/v1/models
```

- 串口结果：

```text
esp-tls: [sock=54] select() timeout
API test failed: class=TIMEOUT ret=ESP_ERR_HTTP_CONNECT errno=0 elapsed=10023ms HTTP=0
```

原因定位：

- Wi-Fi 和 SNTP 已通过，说明不是局域网接入或系统时间问题。
- 请求尚未拿到 HTTP 状态码，失败发生在连接阶段。
- 当前网络到默认 OpenAI Base URL 可能不可达或被阻断。
- 本轮固件默认 `OPENCLAW_API_KEY` 为空，未能进一步验证真实鉴权成功路径。

解决方法：

- 修正错误分类：底层 `ESP_ERR_HTTP_CONNECT` 如果耗满配置 timeout，应归类为 `TIMEOUT`，而不是粗略归类为 `CERT`。
- 新增主机侧测试覆盖“连接失败但耗满 timeout 时应分类为 TIMEOUT”。
- 后续用当前网络可达的 OpenAI-compatible Base URL 和真实 Key 复测。

验证结果：

```text
API test failed: class=TIMEOUT ret=ESP_ERR_HTTP_CONNECT errno=0 elapsed=10023ms HTTP=0
```

后续注意：

- `CERT` 应用于未耗满 timeout 的 HTTPS/证书/连接阶段失败。
- 真正的 API Key 鉴权失败应表现为 HTTP 401/403，并被分类为 `AUTH`。
- 真正跑通目标是 HTTP 2xx 和 TFT `OK`。

## 16. 百度实时 ASR WebSocket 接入与 AppID 错配

问题现象：

- 新增 `ASR TEST` 后，固件能完成录音缓冲、HTTPS 证书校验和 WebSocket 握手。
- 首轮握手未带查询参数时，服务端返回 HTTP 200 非升级响应，ESP-IDF 报 `Sec-WebSocket-Accept not found`。
- 加上 `?sn=...` 后 WebSocket 握手返回 `101`，但发送音频时服务端 reset。
- 修复 WebSocket 帧后，服务端返回：

```text
ASR websocket connected: status=101
ASR start response: {"err_no":-3004,"err_msg":"6: No permission to access data","log_id":...,"type":"FIN_TEXT"}
ASR test failed: start response class=AUTH
```

原因定位：

- 百度实时语音识别 WebSocket 地址需要 `wss://vop.baidu.com/realtime_asr?sn=...`，不能只连 `/realtime_asr`。
- ESP-IDF `esp_transport_ws_send_raw()` 不会自动给 opcode 加 WebSocket `FIN` 位。只传 `WS_TRANSPORT_OPCODES_TEXT` / `BINARY` 会发出未结束帧，服务端会在后续音频阶段断开。
- 加上 `FIN` 位后，START 帧已经被百度服务端解析，曾返回 `err_no=-3004`。这说明设备侧 Wi-Fi、SNTP、证书、WSS、AppID/AppKey 传输和基础 JSON 已经打到服务端鉴权层。
- 后续进一步核对发现，`-3004` 的最终根因不是网络、证书或 `dev_pid`，而是固件实际编入旧 AppID，和当前测试应用 API Key 形成错配组合。

解决方法：

- `ASR TEST` 菜单新增在 `REC TEST` 后。
- 请求前检查 Wi-Fi 必须 `CONNECTED`，SNTP 时间必须有效。
- 录音规格固定为 `16000 Hz / mono / PCM16 / 2 秒`，分 5120-byte binary frame 发送。
- WebSocket 路径增加 `sn` 查询参数。
- START、音频和 FINISH 帧统一使用带 `FIN` 的 opcode：

```text
TEXT | FIN
BINARY | FIN
TEXT | FIN
```

- START 后短读一次服务端响应，串口打印 JSON，并将 `err_no=-3004` 归类为 `AUTH`。
- `ASR TEST` 页面增加 `dev_pid` 现场切换：`VOL-` 选择 `1537` 并重跑，`VOL+` 选择 `15372` 并重跑，`BOOT` 返回菜单。

验证结果：

```text
主机侧逻辑测试：test_baidu_asr_logic 通过
ESP-IDF build：通过，binary size 0x107720，app partition 66% free
刷写：通过，/dev/cu.usbmodem5B5E1028821
Wi-Fi：connected with MadBull, IP 192.168.31.128
SNTP：Time sync OK
ASR TEST：Certificate validated
ASR TEST：WebSocket status=101
ASR TEST：服务端返回 err_no=-3004 / No permission to access data，固件显示 AUTH
ASR TEST：dev_pid=15372 返回 err_no=-3004
ASR TEST：dev_pid=1537 返回 err_no=-3004
ASR TEST：开通付费通道但未充值后立即复测，15372 / 1537 均仍返回 err_no=-3004
ASR TEST：等待约 5 分钟后再次复测，15372 / 1537 均仍返回 err_no=-3004
ASR TEST：核对凭据后发现固件实际编入旧 AppID；修正为当前百度应用 AppID 后，dev_pid=15372 返回 err_no=0 / FIN_TEXT，识别结果为“您好。”
```

后续注意：

- 开发阶段继续使用百度智能云实时语音识别免费额度，正式使用前与 LLM API 一起替换供应商和 Key。
- `-3004` 的最终根因是 AppID/API Key 组合错配：API Key 已是当前测试应用 Key，但固件里仍编入了旧 AppID。
- 修正 AppID 并重新 `idf.py reconfigure`、`build`、`flash` 后，百度实时 ASR 返回 `FIN_TEXT`，说明 Wi-Fi、SNTP、证书、WSS、鉴权和最小 2 秒 PCM16 上传链路已打通。
- 当前 `ASR TEST` 已返回有效 `FIN_TEXT`。后续可以在 `hardware_bringup` 中补齐 ASR->LLM 半闭环和 TTS 单项播放，但不要继续扩展完整 Agent/skill/记忆/提醒系统；上述两项通过后，应把 `hardware_bringup` 冻结为诊断工具，并转入 XiaoClaw 正式集成。
- API Key、AppID、AppKey 等敏感信息不要写入源码或文档明文；开发阶段通过构建环境或配网页面/NVS 注入。

## 17. hardware_bringup 与 XiaoClaw 的职责边界

问题现象：

- 当前 `hardware_bringup` 已经验证了硬件、网络、HTTPS、LLM、录音和 ASR。
- 但仍有两个值得在 `hardware_bringup` 中做完的低层单项验证：ASR->LLM 半闭环、TTS 单项播放。
- 如果在这两个验证之外继续实现完整 ASR/LLM/TTS/Agent 闭环，就容易形成一套临时系统，与最终方案 XiaoClaw 重复。

原因定位：

- 最终方案定为 XiaoClaw，因为它负责本地 skill、记忆、cron、工具调用和正式 Agent 运行。
- `hardware_bringup` 的价值是把硬件/API 风险拆开验证，快速定位底层问题，例如 AppID/API Key 错配、WebSocket FIN 位、音频格式、证书和时间同步。
- 一旦 ASR、LLM、TTS 这些底层风险都完成单项验证，正式产品能力应迁移到 XiaoClaw，而不是继续扩展临时验证固件。

解决方法：

- 将 `hardware_bringup` 定位为长期保留的诊断工具。
- 在 `hardware_bringup` 中只补齐两个剩余验证：
  - ASR->LLM 半闭环：识别文本 -> DeepSeek 最小 chat 请求 -> TFT/串口显示回复。
  - TTS 单项播放：固定文本 -> 云端 TTS -> ESP32 I2S 播放。
- 这两项通过后，不再优先在 `hardware_bringup` 中实现完整语音陪伴闭环。
- 随后开始 XiaoClaw 正式集成：

```text
1. 拉取/阅读/构建 XiaoClaw。
2. 建立本项目 ESP32-S3-N16R8 + 小智扩展板 V2.7 board profile。
3. 迁移已验证的 I2S 麦克风、I2S 功放、TFT、按键、音量、Wi-Fi 配网经验。
4. 在 XiaoClaw 中接入 DeepSeek LLM provider。
5. 在 XiaoClaw 中接入百度 realtime ASR adapter。
6. 在 XiaoClaw 中接入已单项验证通过的 TTS provider。
7. 在 XiaoClaw 上实现正式 ASR->LLM->TTS 闭环。
```

验证结果：

- `hardware_bringup` 已完成关键底层验证：
  - DeepSeek `/chat/completions` HTTP 200。
  - 百度 realtime ASR `FIN_TEXT`。
  - 2 秒 16k mono PCM16 录音缓冲。
  - Wi-Fi、SNTP、HTTPS/WSS 证书链路。
- 剩余底层验证：ASR->LLM 半闭环、TTS 单项播放。

后续注意：

- `hardware_bringup` 可继续用于回归诊断和供应商替换前的单项 API 探测。
- XiaoClaw 才是正式产品系统；skill、记忆、提醒、搜索、飞书同步和完整语音状态机都应在 XiaoClaw 中实现。

## 18. VOICE LLM 半闭环实现与现场复测边界

问题现象：

- `hardware_bringup` 需要在冻结前验证“ASR 文本可以稳定送入 LLM 并返回回复”，但不能扩展成完整 Agent 或接入 TTS。
- 首轮实现后，`VOICE LLM` 可以完成一次 ASR->LLM 成功链路；后续复测中也观察到百度 ASR 在 `FIN_TEXT` 前断开，固件分类为 `TRANSPORT`，TFT 显示 `ASR FAIL / ERR TRANSPORT`。
- 现场连续操作时曾出现一次录音阶段崩溃，回溯落在 2 秒 PCM16 录音缓冲写入路径。

原因定位：

- ASR->LLM 需要把原先只保存 JSON 片段的 ASR 结果改为提取纯 `result` 文本，再作为 OpenAI-compatible chat 的 user message。
- LLM 响应需要从 `/chat/completions` 响应中提取 `message.content` 的短摘要，便于串口输出和后续 TFT 状态判断。
- 录音测试、ASR 测试和 VOICE LLM 测试共用同一个 I2S 麦克风通道，必须避免多个任务同时读取。
- 2 秒 PCM16 缓冲为 64000 bytes，Wi-Fi 启动后内部 RAM 不一定能提供连续 64KB；只用内部 RAM 会导致录音立即失败，只用 PSRAM 又可能在特殊 cache-disabled 窗口下暴露访问风险。
- 对 `TRANSPORT` 追加诊断日志后确认：
  - START 帧完整发送。
  - 13 个 PCM binary chunk 都完整发送。
  - FINISH 帧完整发送。
  - 失败发生在 FINISH 后读取服务端响应阶段，服务端已关闭连接，`errno=119`。
  - 失败样本中第 8 到第 9 个音频 chunk 间隔约 1.5 秒，明显偏离 160ms 实时发送节奏。
  - 当时 Wi-Fi 日志为 `pm start, type: 1`，说明 STA 省电仍开启；实时 ASR 对中途卡顿敏感。

解决方法：

- 新增 `VOICE LLM` 菜单项，位置在 `ASR TEST` 后。
- 新增 `SCREEN_VOICE_LLM_TEST` 和 `voice_llm_state_t`，TFT 显示：

```text
ASR RUN / ASR OK / ASR FAIL
LLM RUN / LLM OK / LLM FAIL
HTTP xxx
ERR <类别>
```

- 将百度 ASR WSS 上传逻辑抽成共享路径，`ASR TEST` 和 `VOICE LLM` 复用同一套 START、PCM binary frame、FINISH 和响应读取逻辑。
- 新增纯逻辑函数和测试：
  - `baidu_asr_extract_result_text()`：从 `FIN_TEXT` JSON 中提取纯 `result` 文本。
  - `api_test_extract_message_content()`：从 chat 响应中提取 `message.content`。
  - `api_test_json_escape()`：转义 ASR 文本后写入 JSON user message。
- `VOICE LLM` 流程固定为：

```text
1. 检查 Wi-Fi CONNECTED
2. 检查 SNTP epoch 有效
3. 打开 I2S 麦克风并向百度 realtime ASR WSS 连续发送 PCM16 音频帧
4. 等待百度 realtime ASR 返回 `FIN_TEXT`
5. 提取 FIN_TEXT.result
6. 调 DeepSeek /chat/completions
7. 串口输出 ASR 文本、HTTP 状态码、回复摘要、错误类别
8. TFT 输出 ASR/LLM 状态、HTTP 状态码和错误类别
```

- 增加录音并发保护：录音任务运行时，其他测试入口不能再次抢同一个 I2S 麦克风通道。
- `ASR TEST` 和 `VOICE LLM` 调整为从 I2S 麦克风直接向百度 realtime ASR WSS 分片发送 PCM16 音频帧，避免在联网后为半闭环测试长期依赖 64KB 连续录音缓冲；`REC TEST` 仍保留 2 秒 PCM16 缓冲作为录音硬件诊断入口。
- STA 连接启动后设置：

```c
esp_wifi_set_ps(WIFI_PS_NONE);
```

  用于关闭 Wi-Fi power save，保证 realtime ASR 上传阶段的音频帧节奏稳定。

验证结果：

```text
主机侧逻辑测试：test_baidu_asr_logic / test_api_test_logic / test_audio_capture_logic 等通过
ESP-IDF build：通过，binary size 0x108920，app partition 66% free
刷写：通过，/dev/cu.usbmodem5B5E1028821
启动：PSRAM OK / MIC READY / SPK READY / KEYS READY
Wi-Fi：connected with MadBull, IP 192.168.31.128
SNTP：Time sync OK
VOICE LLM 成功链路：
  ASR websocket status=101
  ASR response err_no=0 / type=FIN_TEXT
  ASR text=Hello。
  DeepSeek /chat/completions HTTP 200
  LLM reply summary=您好，语音测试中。
VOICE LLM 错误路径：
  ASR websocket status=101
  FIN_TEXT 前连接断开
  固件分类 TRANSPORT
  LLM 未触发
诊断日志：
  失败时第 8 到第 9 个音频 chunk 间隔约 1.5 秒
  修复后 Wi-Fi 日志显示 pm start, type: 0
  修复后 13 个音频 chunk 约 160-170ms 连续发送
修复后复测：
  ASR websocket status=101
  ASR response HEARTBEAT
  ASR response err_no=0 / type=FIN_TEXT / result=你好。
  DeepSeek /chat/completions HTTP 200
  LLM reply summary=你好，语音测试正常。
```

后续注意：

- realtime ASR 上传期间不要开启 Wi-Fi power save，否则可能出现秒级发送卡顿并导致服务端在 `FIN_TEXT` 前断开。
- 后续 XiaoClaw 迁移 realtime ASR adapter 时，也应在语音流上传期间关闭或抑制 Wi-Fi 省电。
- 这一步仍不接 TTS、不做 Agent、不做 skill、不做记忆、不做提醒。
- TTS 单项播放和最终回归通过后，`hardware_bringup` 应冻结为诊断工具。
- API Key、AppID、AppKey 等敏感信息继续只通过环境变量、NVS 或配网页面注入；日志和文档只记录 `SET/EMPTY`、HTTP 状态码和错误类别。

## 19. 最终产品 Wi-Fi 省电与连续多轮对话策略

问题现象：

- `hardware_bringup` 中为保证 realtime ASR 稳定，已全局关闭 Wi-Fi power save。
- 最终产品如果一直关闭 Wi-Fi 省电，会增加待机功耗和发热。
- 如果一直开启 Wi-Fi 省电，又可能导致 ASR/LLM/TTS 的实时网络链路抖动。
- 最终产品还需要支持连续多轮对话，不能每说一句都重新唤醒。

原因定位：

- Wi-Fi 省电适合待机、菜单浏览、普通低频 HTTP 请求。
- realtime ASR、TTS 流式下载或低延迟 LLM 请求需要稳定网络节奏，不适合开启省电。
- 多轮对话需要明确“会话中”和“回到待机”的状态边界。

解决方法：

- XiaoClaw 正式集成时采用会话级 Wi-Fi 省电策略：

```text
待机 / 等唤醒：
  开启 Wi-Fi 省电

唤醒成功 / 准备录音：
  关闭 Wi-Fi 省电

ASR / LLM / TTS / 工具调用期间：
  保持 Wi-Fi 省电关闭

TTS 播放结束：
  进入追问窗口，暂不恢复省电

追问窗口内检测到有效人声：
  继续下一轮对话

追问窗口超时无人说话：
  结束会话，延迟 5-15 秒后重新开启 Wi-Fi 省电
```

- 第一版多轮对话状态机建议：

```text
WAKE_IDLE -> LISTENING -> ASR_RUNNING -> THINKING -> SPEAKING -> FOLLOWUP_WINDOW
FOLLOWUP_WINDOW 有人声 -> LISTENING
FOLLOWUP_WINDOW 超时 -> SLEEP / WAKE_IDLE
```

验证结果：

- `hardware_bringup` 已验证关闭 Wi-Fi 省电后，百度 realtime ASR 音频 chunk 可以稳定按约 `160-170ms` 连续发送，并返回 `FIN_TEXT`。
- 最终产品尚未实现动态开关；这是 XiaoClaw 正式集成阶段的设计要求。

后续注意：

- 第一版追问窗口可先设为 `8 秒`。
- 会话结束后恢复 Wi-Fi 省电前可延迟 `5-15 秒`，避免老人马上补一句时又经历省电切换。
- 后续可增加结束意图识别，例如“没事了”“不用了”“睡吧”。
- 后续可让 LLM 根据回复类型决定追问窗口长短，例如开放式提问给更长窗口，任务确认类回复给更短窗口。
- 建议设置最大连续会话时长，例如 `2-5 分钟`，防止误触发长期占用网络和功耗。

## 20. TTS TEST 单项播放实现

问题现象：

- `hardware_bringup` 在冻结前还缺少“云端 TTS 返回音频后通过 MAX98357A 播放”的单项验证。
- 这一阶段只验证 TTS provider、HTTPS 鉴权、返回音频格式、ESP32 播放和软件音量效果，不能扩展成完整语音闭环。

原因定位：

- 正式产品 TTS 仍倾向选择可迁移声音克隆的 provider，但底层 bring-up 阶段优先普通 TTS，先证明 ESP32 端能拿到可播放音频。
- 若 TTS 返回 MP3/Opus，需要 ESP32 端引入解码链路，当前不适合在诊断工具里扩大范围。
- 百度普通短文本 TTS 支持通过 REST API 请求 PCM，开发阶段可以复用已有百度云账号体系做最小验证。

解决方法：

- 开发阶段 TTS provider 选择百度智能云普通短文本在线合成 REST API。
- 新增 `TTS TEST` 菜单项，位置在 `VOICE LLM` 后。
- 新增 `SCREEN_TTS_TEST` 和 `tts_test_state_t`，TFT 显示：

```text
TTS TEST
BAIDU PCM16
KEY SET / KEY EMPTY
SECRET SET / SECRET EMPTY
RUNNING / OK / FAIL
HTTP xxx
BYTES xxxxx
ERR <类别>
```

- 新增 `baidu_tts_logic.c/.h` 纯逻辑模块和测试：
  - `baidu_tts_url_encode()`：为 form body 编码中文固定文本、token 和 CUID；`tex` 按百度建议做二次 urlencode。
  - `baidu_tts_extract_access_token()`：从 OAuth 响应提取 `access_token`。
  - `baidu_tts_content_is_pcm()`：判断返回 `Content-Type` 是否为 PCM 音频。
  - `baidu_tts_error_label()`：输出短错误类别。
- TTS 流程固定为：

```text
1. 检查 Wi-Fi CONNECTED
2. 检查 SNTP epoch 有效
3. 使用 TTS API Key / Secret Key 请求百度 OAuth token
4. POST https://tsn.baidu.com/text2audio
5. 固定文本：你好，TTS 单项播放测试正常。
6. 请求参数：ctp=1 / lan=zh / spd=5 / pit=5 / vol=8 / per=0 / aue=4
7. 预期返回：audio/basic;codec=pcm;rate=16000;channel=1
8. ESP32 将 PCM16 mono 转 I2S stereo
9. 播放前走现有 mvp_apply_volume() 软件音量缩放
10. 串口输出 token/TTS HTTP 状态、Content-Type、音频字节数和错误类别
```

- 新增编译期环境变量入口：

```text
OPENCLAW_BAIDU_TTS_API_KEY
OPENCLAW_BAIDU_TTS_SECRET_KEY
OPENCLAW_BAIDU_TTS_CUID
```

验证结果：

```text
主机侧逻辑测试：test_baidu_tts_logic 通过
回归逻辑测试：test_baidu_asr_logic / test_api_test_logic / test_api_config_logic / test_audio_capture_logic / test_mvp_logic / test_net_test_logic / test_time_sync_logic / test_wifi_logic 通过
ESP-IDF build：通过，binary size 0x107170，app partition 66% free
上板验证：通过
Wi-Fi：connected with MadBull, IP 192.168.31.128
SNTP：Time sync OK
TTS token：HTTPS 证书校验 OK，HTTP 200，token=SET
TTS audio：HTTPS 证书校验 OK，HTTP 200
Content-Type：audio/basic;codec=pcm;rate=16000;channel=1
音频字节数：114240 bytes
播放：PCM16 mono -> I2S stereo，MAX98357A 播放完成
```

本次现场验证说明：

```text
为避免人工按键，曾临时用 OPENCLAW_TTS_AUTORUN_ON_BOOT=1 构建一次自测固件。
该临时固件在 SNTP OK 后自动进入 TTS TEST。
自测通过后，已重新刷回 OPENCLAW_TTS_AUTORUN_ON_BOOT 未设置的正常菜单触发版。
最终冻结前已使用完整开发阶段 ASR + TTS + DeepSeek 凭据重刷正常菜单触发版；VOICE LLM 和 TTS TEST 均已快速回归通过。
```

后续注意：

- 启动日志、串口日志和文档都不能输出 TTS API Key、Secret Key 或 token 明文，只能输出 `SET/EMPTY`、`token=SET`、HTTP 状态码和错误类别。
- `TTS TEST` 是单项验证，不接 `VOICE LLM`，不做完整语音闭环，不做 Agent、skill、记忆、提醒。
- TTS 单项上板播放和完整凭据回归均已通过；`hardware_bringup` 冻结为诊断工具，后续迁移到 XiaoClaw 正式集成。

## 21. 冻结前最终回归与 TTS PSRAM 接收崩溃修复

问题现象：

- 使用完整开发阶段 ASR + TTS + DeepSeek 凭据重刷正常菜单触发版后，`VOICE LLM` 已通过：ASR WSS `101`，`FIN_TEXT` 非空，DeepSeek `/chat/completions` 返回 `HTTP 200`，回复摘要非空。
- `TTS TEST` 首次最终回归时，百度 OAuth token 已返回 `HTTP 200`，但 `text2audio` 接收音频正文阶段触发 `Guru Meditation Error: Cache disabled but cached memory region accessed`。
- 回溯落在 `tts_audio_event_handler()` 的 `HTTP_EVENT_ON_DATA` 分支，正在把 HTTP 回调中的音频数据 `memcpy()` 到大音频缓冲。

原因定位：

- TTS 音频约 `114240` bytes，诊断固件会优先把大缓冲分配到 PSRAM。
- ESP-IDF HTTP event callback 的执行时机可能处在 cache 关闭窗口；在该窗口内直接写 PSRAM 会触发 `Cache disabled but cached memory region accessed`。
- 该问题只影响 TTS 音频下载接收路径，不影响前面已经通过的 Wi-Fi、SNTP、DeepSeek、ASR realtime WSS 或 I2S 播放逻辑。

解决方法：

- 保留 TTS HTTP event handler 对 `Content-Type` 响应头的记录。
- 移除 event handler 中 `HTTP_EVENT_ON_DATA` 对大音频缓冲的直接写入。
- `baidu_tts_request_pcm()` 改为使用 `esp_http_client_open()` / `write()` / `fetch_headers()` / `read()` 流程，在 TTS task 普通上下文中用 1024-byte internal 栈缓冲读取，再拷贝到音频大缓冲。
- 不设置 `OPENCLAW_TTS_AUTORUN_ON_BOOT`，保持正常菜单触发版。

验证结果：

```text
主机侧逻辑测试：全部通过
ESP-IDF reconfigure build：通过，binary size 0x10a750，app partition 65% free
刷写：通过，/dev/cu.usbmodem5B5E1028821
启动：PSRAM OK / MIC READY / SPK READY / KEYS READY
Wi-Fi：connected with MadBull, IP 192.168.31.128
Wi-Fi power save：pm start, type: 0
SNTP：Time sync OK
API config：base=https://api.deepseek.com model=deepseek-v4-flash key=SET
音量：启动 volume=0
TTS 前：手动 VOL+ 到 volume=10
TTS token：HTTPS 证书校验 OK，HTTP 200，token=SET
TTS audio：HTTPS 证书校验 OK，HTTP 200
Content-Type：audio/basic;codec=pcm;rate=16000;channel=1
音频字节数：114240 bytes
播放：PCM16 mono -> I2S stereo，MAX98357A 播放完成
TTS 后：手动 VOL- 恢复 volume=0
```

最终结论：

- `hardware_bringup` 冻结为硬件/API/音频/网络诊断工具。
- 不再向 `hardware_bringup` 添加完整 Agent、skill、记忆、提醒或正式语音闭环系统。
- 下一阶段开始 XiaoClaw 正式集成：建立 V2.7 board profile，迁移 DeepSeek LLM provider、百度 realtime ASR adapter 和百度普通 TTS provider。
- 当前凭据全部是开发测试用途；产品正式交付前必须手动替换 DeepSeek Key、百度 AppID/AppKey/Secret 等所有线上凭据，并继续避免日志、文档、串口输出明文。

## 22. XiaoClaw openclaw-v2.7 board profile 与本地百度 TTS 接入

时间：2026-05-14

背景：

- `hardware_bringup` 已完成冻结前回归并停止扩展，正式产品能力需要迁移到 XiaoClaw。
- 本阶段目标是先确认 XiaoClaw 能在当前 ESP32-S3-N16R8 硬件上构建、刷写、启动，并建立本项目 board profile。
- GitHub 直连可能失败，因此 XiaoClaw 源码下载使用 GitHub 国内镜像源；工作副本为 `xiaoclaw-ghproxycom`，对应上游项目 `https://github.com/beancookie/xiaoclaw`。

实现内容：

- 新增 `openclaw-v2.7` board profile：
  - V2.7 引脚：`TFT_DC=GPIO40`、`VOL+=GPIO38`、`VOL-=GPIO39`、`BOOT=GPIO0`、`MIC=GPIO4/5/6`、`SPK=GPIO7/15/16`。
  - TFT 使用 ST7735 参数。
  - `BOOT / VOL+ / VOL-` 接入 XiaoClaw board 层；`VOL+ / VOL-` 调整软件音量并保存到 NVS。
  - I2S 麦克风和 I2S 功放使用 Simplex 通道启动。
  - 新增 16MB flash 分区文件 `partitions/openclaw_v2_7_16m.csv`。
- 修改音频路径：
  - `AudioCodec` 启动音量允许保持为 `0`。
  - `AudioService` 增加 PCM 播放入口，用于本地 TTS provider 直接提交 PCM16 数据。
- 新增百度普通 TTS provider 基础实现：
  - 通过 OAuth token 请求百度 `text2audio`。
  - 使用 `esp_http_client_open()` / `write()` / `fetch_headers()` / `read()` 流程读取音频正文。
  - HTTP event callback 只记录 `Content-Type`，避免在 callback 中写 PSRAM。
  - 日志只允许显示 Key/Secret `SET/EMPTY` 或 `token=SET`，不输出明文凭据。
- 将本地 TTS 接入 Mimiclaw Agent 回复路径：
  - 若 `CONFIG_OPENCLAW_BAIDU_TTS_API_KEY` 和 `CONFIG_OPENCLAW_BAIDU_TTS_SECRET_KEY` 已配置，则设备侧请求百度 TTS 并播放 PCM。
  - 若 TTS 凭据为空，则保留原后端 TTS 请求行为。
- 会话级 Wi-Fi 省电策略已完成 board 层基础钩子：
  - 语音链路期间切到 `PERFORMANCE`。
  - 音频通道关闭后延迟约 10 秒恢复 `LOW_POWER`。

验证结果：

```text
idf.py build：通过
xiaozhi.bin：0x319fb0
app partition：0x400000
free：0xe6050，约 22%
刷写：通过，/dev/cu.usbmodem5B5E1028821
启动：ESP32-S3 / 16MB flash / 8MB PSRAM OK
Board：OpenClaw V2.7 board initialized
Audio：NoAudioCodec: Simplex channels created
Wi-Fi：配网 AP Xiaozhi-1279 正常启动
提示音：播放后无崩溃
Release：releases/v2.2.4_openclaw-v2.7.zip 已生成
```

敏感信息检查：

```text
CONFIG_MIMI_SECRET_API_KEY=""
CONFIG_OPENCLAW_BAIDU_TTS_API_KEY=""
CONFIG_OPENCLAW_BAIDU_TTS_SECRET_KEY=""
CONFIG_OPENCLAW_BAIDU_TTS_CUID="openclaw-esp32s3"
```

结论：

- XiaoClaw 已经能在当前 ESP32-S3-N16R8 板子上启动，`openclaw-v2.7` board profile 第一版成立。
- 百度普通 TTS provider 已接入 XiaoClaw Agent 回复路径，但当前构建未写入百度 TTS 凭据，因此本地 TTS 实网请求尚未在 XiaoClaw 中验证。
- 下一步继续接入百度 realtime ASR adapter，把已在 `hardware_bringup` 验证过的 START / PCM binary frame / FINISH / FIN_TEXT 逻辑迁入 XiaoClaw，并形成正式 ASR->LLM->TTS 闭环。

## 23. XiaoClaw 百度 realtime ASR adapter build 级接入

时间：2026-05-14

背景：

- `hardware_bringup` 已冻结为硬件/API/音频/网络诊断工具，只作为已验证流程参考。
- 当前 XiaoClaw 工作副本为 `xiaoclaw-ghproxycom`，目标是在 `openclaw-v2.7` 上接入正式语音闭环的 ASR 入口。
- 敏感信息继续保持不落盘：源码、sdkconfig、日志和文档不得写入测试凭据明文，只显示 `SET/EMPTY` 或 `token=SET`。

实现内容：

- 新增百度 realtime ASR provider：
  - WebSocket 地址使用 `wss://vop.baidu.com/realtime_asr?sn=...`。
  - 发送 `START` 文本帧，随后上传 PCM16 mono 16k binary frames，再发送 `FINISH` 文本帧。
  - 等待 `FIN_TEXT`，从 JSON 中提取 `result` 纯文本作为识别结果。
  - 默认 `dev_pid=15372`，并通过 Kconfig 保留后续可配置空间。
- 新增 ASR 配置项：
  - `CONFIG_OPENCLAW_BAIDU_ASR_APP_ID`
  - `CONFIG_OPENCLAW_BAIDU_ASR_APP_KEY`
  - `CONFIG_OPENCLAW_BAIDU_ASR_CUID`
  - `CONFIG_OPENCLAW_BAIDU_ASR_DEV_PID`
  - `CONFIG_OPENCLAW_BAIDU_ASR_MAX_MS`
  - `CONFIG_OPENCLAW_FOLLOW_UP_WINDOW_MS`
- 接入 XiaoClaw/Mimiclaw 输入链路：
  - 百度 ASR 配置为空时保留原后端 STT 行为。
  - 百度 ASR 配置存在时，本地 ASR 成功后显示用户文本，调用 `bridge_send_to_agent(text)` 进入现有 Agent/LLM 流程。
  - VAD 检测到用户说话后结束本轮 ASR；追问窗口内无人声则回到 Idle。
- 接入会话期 Wi-Fi 省电策略：
  - `Listening` / `Thinking` / `Speaking` 阶段关闭 Wi-Fi 省电，切到 `PERFORMANCE`。
  - `Idle` 阶段恢复 `LOW_POWER`，`openclaw-v2.7` board 层保留约 10 秒延迟恢复。
  - TTS 结束后约 8 秒追问窗口内保持会话性能状态，便于连续多轮。

验证结果：

```text
主机侧 ASR JSON 逻辑测试：通过
ESP-IDF：5.5.2
idf.py build：通过
xiaozhi.bin：0x31c2d0
app partition：0x400000
free：0xe3d30，约 22%
设备端口：/dev/cu.usbmodem5B5E1028821 存在
本次刷写：未执行
原因：当前 ASR/TTS/LLM 测试凭据保持空值，刷机只能验证 fallback，不能验证百度 ASR 实网链路
```

当前敏感配置状态：

```text
CONFIG_MIMI_SECRET_API_KEY=""
CONFIG_OPENCLAW_BAIDU_ASR_APP_ID=""
CONFIG_OPENCLAW_BAIDU_ASR_APP_KEY=""
CONFIG_OPENCLAW_BAIDU_ASR_CUID="openclaw-esp32s3"
CONFIG_OPENCLAW_BAIDU_ASR_DEV_PID=15372
CONFIG_OPENCLAW_BAIDU_ASR_MAX_MS=12000
CONFIG_OPENCLAW_BAIDU_TTS_API_KEY=""
CONFIG_OPENCLAW_BAIDU_TTS_SECRET_KEY=""
CONFIG_OPENCLAW_BAIDU_TTS_CUID="openclaw-esp32s3"
```

结论：

- XiaoClaw 百度 realtime ASR adapter 已完成 build 级接入。
- ASR 文本已经进入 Mimiclaw Agent/LLM 输入链路，具备形成 XiaoClaw 正式语音闭环的代码基础。
- 下一步需要通过本地私有配置写入测试凭据，不提交凭据文件；刷写 `/dev/cu.usbmodem5B5E1028821` 后验证 ASR `FIN_TEXT.result` -> Agent/LLM -> TTS 播放闭环。
- 产品正式交付前必须替换全部测试 API 凭据，并清理或脱敏工作区中的 `测试API合集.rtf`。

## 18. Agent/LLM 栈溢出 — agent_loop 只有 8KB 内部栈

问题现象：

- ASR 文本进入 `bridge_send_to_agent` 后，`agent: Processing message` 出现，随后日志 `A stack overflow in task agent_loop has been detected`，设备重启。

原因定位：

- agent_loop 栈使用 `xTaskCreateStaticPinnedToCore` 从内部 DRAM 堆分配。
- 内部堆碎片化严重（最大连续块 ~9KB），agent_loop 降级到 6-8KB 栈。
- ESP32-S3 Xtensa 端口 `StackType_t = uint8_t`（`portmacro.h:88`），字=字节，不存在字节/字不匹配 bug。
- 真正原因是 `esp_http_client` + mbedTLS 的 TLS 握手调用链需要 ~4-6KB 栈，加上 prompt 构建和 cJSON 操作，6KB 栈不够用。

解决方法（多轮迭代）：

1. 第一步（Codex 遗留）：将 `context_builder.c` 中 `skills[32]`、`always_content[8192]`、`l3_auto[4096]`、`llm_proxy.c` 中 header/read buffer 移到 heap/PSRAM。
2. 第二步：PSRAM 动态栈（24K 字）。解决了栈大小问题，但触发新问题 19。
3. 第三步：内部静态栈（.bss 分配，24K/12K 字）。避开堆碎片，但吃掉了过多内部 SRAM（24K 版只剩 4.7KB，12K 版只剩 8.2KB），连 ASR 音频缓冲都分配不了。
4. 第四步：PSRAM 栈 + Flash 写守卫。在 `agent_loop.c` 中对 `consolidator_check_and_run`、`session_append`、`learning_hook_on_task_end` 三个 Flash 写操作做 PSRAM 栈跳过。同时发现 `bridge.cc` 的 bridge 任务也用了 PSRAM 栈（`MALLOC_CAP_SPIRAM`），修复为内部优先。
5. 第五步（当前方向）：sdkconfig 层面修复 — 降低 `SPIRAM_MALLOC_ALWAYSINTERNAL` 从 16384 到 4096，降低 WiFi 动态缓冲从 32+32 到 8+8，预计释放 ~130KB 内部 SRAM。

涉及文件：
- `main/mimi/agent/agent_loop.c`：PSRAM 回退、Flash 写守卫、栈诊断日志、LLM 前栈水位检查
- `main/mimi/agent/context_builder.c`：大对象 heap/PSRAM 迁移
- `main/mimi/llm/llm_proxy.c`：header/read buffer heap/PSRAM
- `main/mimi/mimi.c`：agent_loop_start 优雅失败（不用 ESP_ERROR_CHECK）
- `main/bridge/bridge.cc`：bridge 栈改为内部优先
- `main/mimi/mimi_config.h`：MIMI_AGENT_STACK=24K

验证结果：

- ASR 链路：WebSocket 101 ✅, MID_TEXT ✅, FIN_TEXT.result ✅, Local ASR text ✅, bridge_send_to_agent ✅
- Agent 收到消息：`agent: Processing message from xiaozhi:local` ✅
- Bridge 返回 thinking：收到 "思考中..." 回复 ✅
- AGENT_LOOP 创建：`stack=24576 words (24576 bytes, psram)` ✅
- free SRAM：32047 bytes ✅
- 凭据保护：`appid=SET appkey=SET` ✅
- LLM 调用 / TTS 播放：在一次按键测试中设备重启，回溯因串口溢出丢失 ❓

后续注意：

- 优先尝试 sdkconfig 内存优化方案（问题 20），从根本上解决内部 SRAM 不足。
- ESP32-S3 的 512KB 内部 SRAM 是本项目最大硬件约束，所有内存决策都需以此为边界。

## 19. PSRAM 栈触发 Flash 缓存断言崩溃

问题现象：

- agent_loop 使用 PSRAM 栈后，在 Flash 写操作（session 保存、memory 更新）时崩溃：
  ```
  assert failed: spi_flash_disable_interrupts_caches_and_other_cpu
  cache_utils.c:127 (esp_task_stack_is_sane_cache_disabled())
  ```

原因定位：

- ESP-IDF 在 Flash 写/擦除操作时会禁用 SPI Flash 缓存（因为 Flash 和 PSRAM 共享 SPI 总线）。
- 缓存禁用后，CPU 无法访问 PSRAM（PSRAM 需要缓存工作）。
- `esp_task_stack_is_sane_cache_disabled()` 检查当前任务栈是否在内部 SRAM——如果在 PSRAM，断言触发。
- FATFS 读（prompt 构建）不触发缓存禁用，PSRAM 栈安全。FATFS 写（session 保存、consolidator、learning hooks）触发缓存禁用，崩溃。

解决方法（参见问题 18 第四步）：

- 在 `agent_loop.c` 中增加 `s_agent_psram_stack` 标志。
- 三个 Flash 写操作点增加守卫：`consolidator_check_and_run`、`session_append`、`learning_hook_on_task_end`。
- Bridge 任务（`bridge.cc`）原使用 `MALLOC_CAP_SPIRAM` 分配栈，也与 Agent 回复处理中的 Flash 写冲突。改为内部优先 + PSRAM 回退。

验证结果：

- PSRAM agent_loop 栈创建成功，系统启动无崩溃。
- ASR → Agent 处理 → Bridge thinking 回复通过。
- 一次按键测试中设备仍在 LLM/TTS 阶段重启，可能有未覆盖的 Flash 写路径。

后续注意：

- 还存在其他可能使用 PSRAM 栈且执行 Flash 写的任务吗？检查所有 `MALLOC_CAP_SPIRAM` 的栈分配。
- ASR 任务也使用 PSRAM 栈（`BaiduAsrProvider: ASR task created with stack=12288 bytes (psram)`）——ASR 任务是否也有 Flash 写？
- 终极解决方案是 sdkconfig 内存优化（问题 20），让 agent_loop 直接用内部栈。

## 20. 内部 SRAM 严重不足 — 根本瓶颈诊断

问题现象：

- ESP32-S3-N16R8（16MB Flash, 8MB PSRAM）启动后内部 SRAM 仅剩 ~32KB。
- Firmware 和各子系统初始化后，free sram 波动在 30-35KB，minimal sram 低至 1.8KB-3KB。
- 大量 ≤16KB 的堆分配走内部 SRAM 而非 PSRAM，加速碎片化。

原因定位（SRAM 预算分解见 `START_HERE_项目速览.md`）：

1. **`CONFIG_SPIRAM_MALLOC_ALWAYSINTERNAL=16384`**（头号浪费）：所有 ≤16KB 的 `malloc` 都走内部 DRAM，不走 8MB PSRAM。Agent 提示词碎片、cJSON 节点、HTTP 缓冲、临时字符串等大量 1-16KB 的分配全部挤在内部 SRAM。
2. **WiFi 动态缓冲 32+32**：每个 ~1.6KB，共 ~102KB。语音设备实际不需要这么多 WiFi 缓冲。
3. **`CONFIG_SPIRAM_MALLOC_RESERVE_INTERNAL=32768`**：锁定 32KB 内部 SRAM 不让 PSRAM 分配用。
4. **ESP-IDF 框架开销**：CPU 缓存 48KB + 系统任务栈 20KB + LWIP 11KB + FreeRTOS 内核 ~50KB + IRAM 118KB + DRAM 34KB = ~280KB。
5. 综合碎片化后实际可用仅 30-35KB，Agent 栈（需 12-24KB）+ ASR 音频缓冲（~10KB）已超出余量。

解决方法（建议，尚未实施）：

```text
# sdkconfig 修改
CONFIG_SPIRAM_MALLOC_ALWAYSINTERNAL=4096    # 从 16384 降低到 4096
CONFIG_ESP_WIFI_DYNAMIC_RX_BUFFER_NUM=8     # 从 32 降低到 8
CONFIG_ESP_WIFI_DYNAMIC_TX_BUFFER_NUM=8     # 从 32 降低到 8
```

预计效果：

- 释放 ~130KB 内部 SRAM（SPIRAM 阈值: ~80KB + WiFi: ~50KB）。
- 可用从 32KB → ~160KB。Agent 栈、ASR 缓冲、TLS 握手都可以直接用内部 SRAM。
- 不再需要 PSRAM 栈和 Flash 写守卫等变通方案。
- free sram 预期从 32KB 提升到 160KB+。

验证结果：待实施。

后续注意：

- 实施前备份 sdkconfig。
- 降低 WiFi 缓冲数可能影响高并发场景，但语音设备单连接足够。
- `SPIRAM_MALLOC_ALWAYSINTERNAL=4096` 意味着 ≥4KB 的分配走 PSRAM，≤4KB 走内部。Audio/DMA 缓冲（通常 >4KB）会走 PSRAM，内部留给任务栈和关键小分配。
- 如果 PSRAM 速度成为瓶颈（80MHz Octal PSRAM 带宽约 80MB/s，对语音设备足够），可考虑进一步降低阈值或调整其他参数。

## 21. 内存问题最终解决——PSRAM 化 + 内部栈 + 预读缓存

问题现象：

- 经过 20+ 轮尝试，ESP32-S3 内部 SRAM 总是不够——PSRAM 栈触发 Flash 断言、内部栈溢出、TLS DMA 缓冲失败、SRAM 碎片化。反复在"修好一个又坏另一个"之间循环。

原因定位：

- 根本原因：`context_builder.c` 中 3 个 `file_cache_t{content[4096]}` = 12KB 在 .bss 段（内部 SRAM），挤占了大块连续空间，导致 `internal_largest` 仅 7.6KB。
- PSRAM 不能当栈用：ESP-IDF 禁止 PSRAM 栈任务碰 Flash（含 FATFS 读），因为 SPI Flash 后台磨损均衡会禁用缓存 → PSRAM 不可访问。
- SPI Flash 总线上 PSRAM 和 Flash 共享缓存：缓存禁用期间 PSRAM 不可用，所以 PSRAM 栈 + FATFS = 必崩。
- mbedTLS DMA 需要内部 SRAM：TLS 加密操作使用 DMA，PSRAM 缓冲会导致 `No more processes` 错误。
- 多次尝试中发现的子问题：
  - Bridge 任务也用 PSRAM 栈 → Flash 回调断言（已修复为内部栈）
  - agent_loop 中的 `session_get_history_json` fopen FATFS → PSRAM 栈断言（已 guard）
  - DeepSeek API URL 缺少 `/v1` → 404 重定向 → 4 分钟超时（已自动修正）
  - MCP 工具名含 `.` 不合法 DeepSeek 命名规范（已改为 `_`）
  - SNTP 从未初始化 → 系统时间 1970 → HTTPS 证书校验失败（已修复）

解决方法：

**核心原则：能放 PSRAM 的放 PSRAM，必须内部 SRAM 的留在内部。**

| 数据 | 位置 | 策略 |
|---|---|---|
| agent_loop 任务栈 16KB | 内部（提前分配） | FreeRTOS 栈，碰 Flash 必须内部 |
| prompt 文件缓存 3×4KB | **PSRAM**（heap alloc） | 纯文本缓冲，不碰 DMA/ISR |
| DMA 保留池 32KB | 内部 | TLS/I2S DMA 必须 |
| mbedTLS 分配 | DEFAULT 模式 | 跟全局 malloc，优先内部 |
| 运行时 heap 分配 | SPIRAM_ALWAYSINTERNAL=16384 | ≤16KB 走内部，>16KB 走 PSRAM |

关键文件改动：

1. context_builder.c：`file_cache_t.content[4096]` → `char *content`（PSRAM malloc）。新增 `context_preload_files()` 在 mimiclaw_init（内部栈）预读 FATFS，`cached_read()` 移除 TTL/fopen，永不碰 FATFS。
2. mimi.c：`agent_loop_start()` 在子系统初始化前调用（抢大块内部堆），不用 ESP_ERROR_CHECK。
3. agent_loop.c：MIMI_AGENT_STACK=16K，agent_alloc_stack 内部优先+PSRAM 回退，session/consolidator/learning hooks 在 PSRAM 时跳过。
4. application.cc：WiFi 连上后 `esp_netif_sntp_init("pool.ntp.org")`。
5. llm_proxy.c：DeepSeek URL 自动补 `/v1`。
6. tool_mcp_client.c：工具名 `mcp_server.tools_call` → `mcp_server_tools_call`。

验证结果：

```
free sram:        32KB → 39KB
internal_largest: 7.6KB → 19.4KB (+153%)
agent 栈:         6KB 内部 → 16KB 内部
LLM 调用后:       stack_free=8688 bytes
LLM 返回:         "你好！我是小龙虾，有什么可以帮你的吗？"
TTS:              HTTP 200, 4497 bytes audio (PCM 播放 TRANSPORT 待修)
```

ASR → LLM → TTS 闭环已上板验证通过。

后续注意：

- PSRAM 是"能放才能放"——必须确认数据不用 DMA、不进 ISR、不在 Flash 操作期间被访问。
- 不要尝试 BSS-in-PSRAM 全局开关：ESP-IDF 没有精细到能分辨每个全局变量是否 DMA/ISR 安全。
- 迁移 PSRAM 的策略是**个案分析**：每个 buffer 手动确认安全后单独迁移。

## 24. TTS PCM 播放 TRANSPORT 错误 → 最终溯源至 ESP-IDF mbedTLS bug

问题现象：

- ASR → LLM → TTS 闭环中，百度 TTS API 返回 HTTP 200 + Content-Length ~200KB，但 ESP32 只收到 3608 bytes 后即报 `class=TRANSPORT ret=ESP_FAIL`。
- 本机 curl 测试确认百度 API 完全正常，返回完整音频。

原因定位过程（经 15+ 轮上板测试）：

1. 确认不是格式问题：aue=4(PCM) 和 aue=6(WAV) 表现一致，都在 ~3.6KB 截断。
2. 排除应用层代码路径：open/write/read 和 esp_http_client_perform 表现一致。
3. 排除 TLS 配置：关 TLS 重协商、增大 mbedTLS buffer(16K→32K)、增 HTTP buffer、关 keep-alive 均无效。
4. 排除超时：timeout_ms 设到 60s 也无用，实际在 ~600ms 就返回 `ESP_ERR_HTTP_INCOMPLETE_DATA`。
5. 尝试 Baidu 流式 TTS（WebSocket wss://）：电脑端收到 29664 bytes 完整音频 + system.finished。ESP32 端收到 4-8 帧碎片（3533-6992 bytes），无 finished 帧，mbedTLS 报 `MBEDTLS_ERR_SSL_INVALID_RECORD (-0x7100)`。
6. 增大 WS_BUFFER_SIZE(1024→32768) 无效。
7. 5 秒延迟发 system.finish 后，电脑端正常收完整数据，ESP32 端依然截断。
8. 对照 ESP-IDF 官方已知 bug #11833：同样错误，同样症状。

根本原因：ESP-IDF v5.5.2 的 mbedTLS 在接收大 TLS record（>4KB）时，`mbedtls_ssl_fetch_input()` 返回 `MBEDTLS_ERR_SSL_INVALID_RECORD`。此 bug 跨 HTTP POST 和 WebSocket 两种传输模式，应用层无法绕过。

最终决策：放弃 ESP32 端直接调百度 TTS/ASR API。改为 ESP32 瘦客户端 + 后端服务器胖服务端架构。详见 `架构方案_后端服务器_XiaoClawBrain.md`。

## 25. 架构方案变更：ESP32 瘦客户端 + 后端服务器

时间：2026-05-16

背景：

- 问题 #24 确认 ESP-IDF mbedTLS bug 无法在应用层修复。
- xiaoclaw 上游本身就设计为 ESP32 终端的后端服务器架构，ASR/TTS/LLM 全部在服务端处理。
- 当前 ESP32 端已存在 WebSocket 通道（protocols/websocket_protocol.cc），`SendBackendTtsRequest()` 和 `OnIncomingJson` 回调已就绪。

方案概要：

```
ESP32-S3 (纯终端)             后端服务器 (Python)
─────────────────             ─────────────────
麦克风 → Opus编码 → WebSocket → 百度 ASR API
显示屏 ← JSON ← WebSocket    ← LLM Agent (DeepSeek)
功放 ← Opus解码 ← WebSocket  ← 百度 TTS + Opus编码
按键 → JSON → WebSocket      → Skill 引擎
                              → 记忆 / Cron / MCP
```

后端底座：xiaozhi-esp32-server（Python，5.9K stars，小智协议原生兼容）
增量模块：skill_loader.py + agent_loop.py + cron_scheduler.py（共 ~430 行）

ESP32 侧改动：
- 删除：providers/（本地 ASR/TTS）、mimi/（Agent）、bridge/
- 保留：audio/（编解码）、display/（TFT）、protocols/（WS）、boards/（硬件）
- 配置：NVS 写入 `websocket.url` = 后端地址

开发期部署：Mac Mini M4，手动启动。
生产期部署：腾讯云轻量 2核4G，~65元/月。

完整方案文档：`架构方案_后端服务器_XiaoClawBrain.md`

## 26. P4 日志脱敏 Bear token 泄漏

问题现象：

- `sanitize_text("Authorization: Bearer abcdef1234567890")` 输出 `Authorization=SET abcdef1234567890`
- token `abcdef1234567890` 完整泄漏在日志中

原因定位：

- `sanitize_text()` 中先执行 `BEARER_PATTERN.sub()` 将 `Bearer abcdef1234567890` 替换为 `Bearer abc***890`
- 但替换后的 `Authorization: Bearer abc***890` 被 `SECRET_PATTERN` 再次匹配
- `SECRET_PATTERN` 的 pattern 是 `(?i)(authorization|...)\s*[:=]\s*([^\s,;]+)`，匹配到 `Authorization: Bearer`（`=` 被替换为 `:` 后的格式）
- `SECRET_PATTERN` 的 Group 2 捕获的是 `Bearer` 而非原始 token，因为 `Bearer` 不含空格，符合 `[^\s,;]+`
- 所以 `Authorization=` 被替换为 `Authorization=SET`，后面的 token 被原样保留

解决方法：

- `sanitize_text()` 改用正则 `r"(?i)^(authorization)\s*[:=]\s*(Bearer\s+)(.+)$"` 对 `Authorization: Bearer <token>` 做精确匹配
- 只对 group(3) 的 token 调用 `mask_secret()`，其余部分原样保留
- 独立函数 `sanitize_authorization(value)` 处理 Authorization 头的 Bearer 格式
- `sanitize_mapping(data)` 对 Authorization 字段调用 `sanitize_authorization()` 而非通用的 `mask_secret()`

验证结果：

```python
sanitize_text("Authorization: Bearer abcdef1234567890")
# 输出：'Authorization: Bearer abc***890'  ✅ token 不泄漏

sanitize_text("Authorization=Bearer abcdef1234567890")
# 输出：'Authorization: Bearer abc***890'  ✅ 兼容 = 分隔符

sanitize_text("api_key=abcdef1234567890")
# 输出：'api_key=abc***890'  ✅ 其他 key 不受影响
```

后续注意：

- Bearer token 的 word boundary (`\b`) 在 Python re 中对中划线敏感，`Bearer` 后是中划线而非字母/数字，`\b` 不会跨中划线匹配，所以单独保留 `Bearer\s+` 在正则中是正确的
- Bearer token 字符集包含 `.`、`_`、`~`、`+`、`/`、`=`、`-`，在 `[A-Za-z0-9._~+/=-]+` 范围内已覆盖

## 8. P5.1 状态机 BOOT 路径被拒绝 + 旧音频链路误触发

问题现象：

P5.1 首次验收时发现 BOOT 按键 PressDown → `SetDeviceState(kDeviceStateWakeupDetected)` → `SetDeviceState(kDeviceStateListening)` 后状态机返回 `false`（无效转换），同样 `Listening → Recognizing` 也被拒绝。另外进入 `Listening` 后 TFT 状态虽然显示了，但旧协议的 `protocol_->SendStartListening()` 和 `EnableVoiceProcessing(true)` 仍被执行，可能触发真实音频上传。

原因定位：

1. `DeviceStateMachine::IsValidTransition()` 中 `Idle` 的目标没有包含 `WakeupDetected`，`Listening` 的目标没有包含 `Recognizing`，导致 BOOT 状态流半途卡住。
2. `HandleStateChangedEvent()` 中 `kDeviceStateListening` case 无条件执行 `protocol_->SendStartListening()` 和 `EnableVoiceProcessing(true)`，没有区分 BOOT 手动入口和语音自动入口。

解决方法：

1. 补齐 `device_state_machine.cc` 中的状态转换规则：
   - `Idle` → `WakeupDetected`、`Reconnecting`
   - `Connecting` → `WakeupDetected`
   - `Listening` → `UploadingAudio`、`Recognizing`、`Thinking`、`Speaking`、`Idle`
   - `WakeupDetected` → `Idle`
   - `UploadingAudio` → `Thinking`、`Synthesizing`
   - `Recognizing` → `Synthesizing`
   - `Thinking` → `Synthesizing`
   - `Synthesizing` → `Idle`

2. 新增 `application.h`：`SetXiaoClawBootListening(bool)` setter，在 BOOT PressDown/Release 时设置/清除 `xiaoclaw_boot_listening_` 标志。

3. `openclaw_v2.7_board.cc` BOOT 入口：PressDown 时先 `SetXiaoClawBootListening(true)` 再 SetDeviceState/SendListenStart；Release 时先 SendListenStop/SetRecognizing，再 `SetXiaoClawBootListening(false)`。

4. `HandleStateChangedEvent` 中 `kDeviceStateListening` case 入口加 guard：
   ```cpp
   if (xiaoclaw_boot_listening_) {
       audio_service_.EnableVoiceProcessing(false);
       audio_service_.EnableWakeWordDetection(false);
       break;
   }
   ```
   P5.1 BOOT 路径只更新 TFT + 发 JSON，不走旧音频上传链路。

5. `xiaoclaw_ws_client.h` 新增 `OnError` 回调，`HandleErrorMessage()` 末尾调用；`application.cc` 中 `OnError` 注册 → display 显示 "error" + 状态切 `kDeviceStateError`。

6. `InitializeXiaoClawWsClient()` 连接失败时 display 显示 "reconnecting" + 状态切 `Reconnecting`，不再静默 reset。

7. 新增独立 `kDeviceStateError`，协议 `error` 不再借用 `kDeviceStateFatalError`；`error` 状态允许恢复到 `idle` 或 `reconnecting`。

验证结果：

```bash
idf.py build  # Project build complete. xiaozhi.bin binary size 0x2b9170 bytes
rg -n '"type":\s*"tts".*"audio"|base64_opus|Authorization: Bearer [A-Za-z0-9._~+/=-]{12,}...'
# No secrets found
```

BOOT PressDown → `idle → wakeup_detected → listening`，Release → `recognizing`，状态机转换全部放行，HandleStateChangedEvent 中旧音频链路被 `xiaoclaw_boot_listening_` 标志拦截。

后续注意：

- BOOT 按键的 PressDown/Release 需要硬件支持；本项目 `button` 组件支持这两个事件

## P5 上板验收根因补修（2026-05-21）

### 问题 A：BOOT Release 后 Opus 编码帧未上传

问题现象：
- BOOT 按下 180ms 后松开，Opus 编码器缓冲还没 flush 完
- `OnOpusFrameFromAudio` 入口日志缺失，无法确认 callback 是否被调用
- WS `SendOpusFrame` 无第一帧日志，无法确认上传是否真正发生

原因定位：
- `OnOpusFrameFromAudio` 在 `uploading_enabled_ == false` 时静默 return，无日志
- BOOT Release 时立即 `XiaoClawStopAudioUpload()` 关闭上传，但 Opus encoder 仍有缓冲帧未产出
- 上传关闭到编码器 flush 之间的时序 gap 导致帧被静默丢弃

解决方法：
- `OnOpusFrameFromAudio` 加限流 drop 日志（bad_state / no_ws / upload_disabled 各单独计数）
- `SendOpusFrame` 加第一帧日志（WS client 层）
- BOOT Release 改为 `esp_timer_start_once(upload_flush_timer_, 1.5s)`，不立即关闭上传
- Timer callback 只 `Schedule()` 到 Application 主任务，由主任务检查状态后执行关闭
- `OnPressDown` 先 `esp_timer_stop()` 取消上一次 flush timer

验证结果：
- `idf.py build` 干净通过，无 warning
- 上板后期望看到 `FIRST opus frame uploaded len=...` 和 `opus cb OK count=1`

### 问题 B：WS TTS binary 帧未触发播放

问题现象：
- 后端发送了 `tts_start` / `tts_stop`，但串口无 binary 帧日志

原因定位：
- `application.cc:408` 已正确设置 `SetTtsBinaryCallback([this](...){ OnTtsBinaryFrame(...) })`，回调链未断
- 但 `HandleWsData` binary 分支无第一帧日志，无法确认 binary 帧是否真的到达 WS 层
- `OnTtsBinaryFrame` 在 bad_state 时静默 return，无日志

解决方法：
- `HandleWsData` binary 分支加第一帧 + 每 25 帧计数日志（rate-limited）
- `OnTtsBinaryFrame` 加第一帧日志 + 限流 bad_state drop 日志
- `XiaoClawWsClient` 加 `tts_binary_frame_count_`/`tts_binary_bytes_` 成员，`OnTtsStart` 时 reset
- 打印后端 codec 参数供后续判断是否需要调整 sample_rate

### 问题 C：后端 idle 时主动 TTS 被状态机拒绝

问题现象：
```
StateMachine: Invalid state transition: idle -> synthesizing
XiaoClawWS: tts start received
```

原因定位：
- 后端可能在 device 处于 `idle` 时主动发 TTS（版本检查失败等场景）
- 状态机 `kDeviceStateIdle` 的 `IsValidTransition` 不允许直接迁到 `kDeviceStateSynthesizing`

解决方法：
- `device_state_machine.cc` 的 `kDeviceStateIdle` case 加 `to == kDeviceStateSynthesizing`

### 问题 D：recognizing 状态无超时回退

问题现象：
- BOOT Release 后进入 `recognizing`，后端无响应时一直等待
- 后续 BOOT 按键被拒绝，无法重新开始

原因定位：
- `recognizing` 进入后没有任何 timer，超时全靠后端响应
- 状态机允许 `recognizing → idle`，但没有触发机制

解决方法：
- `Application` 成员 `xiaoclaw_recognizing_timeout_timer_`，构造时创建，析构时销毁
- 状态机 listener：进 `recognizing` 时 `StartRecognizingTimeout()`；进 `thinking/synthesizing/speaking/idle/error/reconnecting` 时 `CancelRecognizingTimeout()`
- watchdog 后 TTS 队列中已在处理中的句子仍会播完（额外 1~2 句），可后续增加裁剪

---

### 问题 M：Fast Path time_words 过宽 + `actual_text` JSON 覆盖 Bug

问题现象：
- "现在几点了？"偶发未命中 fast path，走了 LLM 慢速回复
- 同时上一轮修复错误地把 `什么时候`/`啥时间`/`几时` 纳入 time_words，导致 `你什么时候有空`、`现在什么时候出发` 等非时间问句也走了本地快答

原因定位：
- 上一版（2026-05-28 第一版）企图通过扩充 time_words 解决漏配，但规则过宽误伤了正常问题
- 根本的偶发未命中原因不是匹配规则窄，而是 `startToChat()` 在 JSON 输入时 `actual_text` 被原始 JSON 覆盖（`actual_text = text`），导致 `get_fast_path_reply` 收到整段 JSON 而非 `content`
- 当 JSON 输入包含 `{"speaker":"user1","content":"现在几点了？"}`，JSON 含有标点和英文，punctuation strip 后仍保留"几点"所以恰好在某些情况能匹配，但整体协议一致性被破坏

解决方法：
- `fastPath.py`：time_words 只保留 `"几点"`，去除所有扩展词
- `receiveAudioHandle.py`：去掉 JSON 分支末尾的 `actual_text = text` 重置，使 `get_fast_path_reply` / `send_stt_message` / `conn.chat` 都使用提取后的 `content`
- `language_tag = data["language"]` → `data.get("language")` 防止缺字段抛 KeyError

验证结果：
- `python3 -m unittest tests.test_fast_path`：34/34 通过
- `python3 -m unittest discover -s tests`：60/60 通过
- 集成测试确认 JSON 输入后 fast path 收到 content、send_stt 发 content、不走 executor

涉及文件：
- `xiaozhi-esp32-server/main/xiaozhi-server/core/handle/fastPath.py`
- `xiaozhi-esp32-server/main/xiaozhi-server/core/handle/receiveAudioHandle.py`
- `xiaozhi-esp32-server/main/xiaozhi-server/tests/test_fast_path.py`
